# Third-party dependencies of unmassk-trading's own scripts.
# The rest of this toolkit is stdlib-only; these two arrive with the lifted
# discipline layer (see CREDITS.md) and are declared here rather than assumed.
#
#   python3 -m venv <workdir>/venv
#   <workdir>/venv/bin/pip install -r "<this plugin's directory>/requirements.txt"
#
# A bare `pip` is not on every machine, and `pip3` often refuses with
# externally-managed-environment. The venv avoids both, and the scripts are then run
# with <workdir>/venv/bin/python. `${CLAUDE_PLUGIN_ROOT}` does NOT work here: it is
# empty in the shell (see the skill's own Paths note).
#
# Not needed to use the skill's knowledge layer, nor to drive kraken-cli.
# Needed to run this plugin's own test suite and its gate scripts.

PyYAML>=6.0          # check_circuit_breaker.py, check_pre_trade_discipline.py
jsonschema>=4.25.1   # thesis_store.py — validates every write against schemas/thesis.schema.json
