---
description: Put this project's rules file on screen
allowed-tools: Read
disable-model-invocation: true
---

Read `.claude/project-memory/rules.md` — this project's rules file — **whole**,
every line. It is for YOU, not for the user: they wrote it and do not need it
read back. **Never print the rules.** Answer with one line saying how many are
loaded, plus — only if it applies — one short line per rule that contradicts
something you already said or did in this session, and the correction you are
making because of it.

From this point in the session those rules are binding, exactly as if the user
had just typed each one.

If the file does not exist, say the toolkit is not installed in this project.
Never report "there are no rules" when what happened is that you could not look.
