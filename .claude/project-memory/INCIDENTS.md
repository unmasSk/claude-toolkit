# INCIDENTS — índice. Lo escribe el script. No editar. Si diverge, manda git.

[I-001][testing][ci] Ubuntu CI flakiness on #61 was reopened, then closed for real
[I-002][memory][testing] building v2 found three silent-loss bugs -- fixed, now regression tests
