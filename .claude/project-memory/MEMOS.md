# MEMOS — índice. Lo escribe el script. No editar. Si diverge, manda git.

[M-001][memory][skills] activating memory in a project is a plan, tracked in issue 82
[M-002][install][memory] config.json repo_type is deduced from the branches, never asked
