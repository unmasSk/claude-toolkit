---
name: review-history
description: Dated log of past Cerberus commit-reviews/audits with full verdicts, evidence, and test counts — detail moved out of MEMORY.md to keep the index compact
metadata:
  type: project
---

- 2026-08-02 feat/memoria-v2 capa-4 close §12bis paso 3 review (3 files: `lib/memory/report.py`, `report_render.py`, `boot.py`, against PIEZAS.md §0/§2/§9.2/§9.3/§9.5 + TEXTOS.md §2.1-2.3/§3.1-3.2): NOT MERGEABLE — 1 T1 (`health.coherence_rules()`, written same day to close a live-demonstrated silent rule-loss bug, is excluded from `HealthReport`/`boot._avisos_block()` because `model.HealthReport` only has 4 fields — the guard exists, passes its own tests, but never reaches the one surface a human reads every session) + 5 T2 (3 real duplicates: `report.py::_pm_root()`≡`notes.py::pm_root()` byte-identical disclosed-but-unresolved; `report.py::_archived_ids()`≡`boot.py::_archived_ids()` disclosed one-liner; `report_render.py::_utc_label()`≠`boot.py::_utc_label()` UNDISCLOSED drift, one has defensive `.astimezone()` one doesn't — plus stale docstring prose in `report.py:25-38` narrating the `notes.py` index-location bug as current when it was fixed the same day, plus `report_render.py::render()` zero-test/zero-caller wrapper existing only to satisfy `vocabulary.py`'s single-string `FieldSpec.reader` for a field two functions read) + 2 T3 (`boot.py`'s disclosed 2-line omission from TEXTOS §3.2 for barred `zones` import; `report_render.py`'s 5 disclosed cosmetic deviations, all blocked on `model.py` not carrying the needed fields). All 3 files under the 500-line cap (220/446/283), zero try/except/silent-swallow, 137/137 `tests/memory` green. See [anti-patterns.md](anti-patterns.md) "feat/memoria-v2 — report.py/report_render.py/boot.py (capa 4 close, §12bis paso 3, 2026-08-02)" entry.
- 2026-08-02 feat/memoria-v2 capa-4 §12bis review (5 files: `lib/memory/clusters.py`, `context.py`, `rules.py`, `health.py`, `dispatch.py`, against PIEZAS.md §9.1/§9.4/§9.6/§9.7/§9.8 + §0/§2 + TESTIGO.md §3): NOT MERGEABLE — 2 real duplicates confirmed (Jaccard tokenize+overlap `similar.py`↔`rules.py`, plus a 3rd independent `0.5` threshold constant in `validator.py`; hand-rolled empty-commit git invocation `context.py`↔`rules.py`, a 3rd copy of knowledge `gitcmd.commit()` already encodes) + 2 new correctness findings (`rules.py::add()` commits to git FIRST then writes `rules.md` SECOND with no rollback on file-write failure — reverses `notes.py::write()`'s own "no negociable" write-index-then-commit-with-rollback order, and `notes.py`'s own docstring literally names this class of bug "ES LA PIEZA DONDE EL SISTEMA SE PUEDE CORROMPER A SI MISMO"; `dispatch.zone_of()` silently falls back to word-matching when an explicit `Zone:` line names an unresolvable zone, contradicting its own "manda siempre" contract) + 1 contract-consistency gap on the 3 self-declared deviations (`health.py` skipping `duplicates()`/`build()` is legitimate against its OWN test table but contradicts `ids.py`'s already-written ficha, which names `health.duplicates` as its declared caller — leaves `ids.find_duplicates` an orphan and the boot's "✓ IDs sin duplicados" line unproducible). All 5 files clean on signature-vs-contract (zero undeclared public exports), all under both the 500-line project cap and the 300-line generic default. `clusters.py` verified correct with no findings (union-find root/child resolution matches the documented Origin/Replaces semantics exactly, including the orphan-pointer and cycle-safety-net edge cases). See [anti-patterns.md](anti-patterns.md) "Capa 4 memoria-v2 — clusters.py/context.py/rules.py/health.py/dispatch.py" entry.
- 2026-08-02 feat/memoria-v2 boot-surgery §5.3 authorization review (5 files: `lib/boot_git_checks.py` (-535L), `lib/boot_render.py` (-244L), `hooks/session-start-boot.py` (-159L), `lib/boot_health.py` (-70L), `lib/boot_checks.py`, against the 10 governing docs incl. `PLAN-CONSTRUCCION.md` §5.3, `DEUDA.md`, `DRIFT.md`, `spec-sistema-memoria-v2.md` §8.3): NOT MERGEABLE — 0 NO AUTORIZADO (every removed function maps 1:1 to §5.3's own column), 2 PÉRDIDA COLATERAL (one already tracked, `DEUDA.md` #6 `check_upstream_shares_history()`, reconfirmed live via the diff's own docstring text; one NEW and undocumented: `fetch_memory_ref()`'s removal deletes the ONLY `git fetch` anywhere in the boot path — `grep -rn '"fetch"' lib hooks bin` outside `lib/memory/` = zero hits — silently staling `BRANCH`'s ahead/behind, the `PULL DIRECTIVE`, and `BRANCHES`, all three explicitly listed as "se queda" in the same §5.3 row; the plan itself, not just the execution, has the blind spot). Confirmed HEAD (`177420b`) is currently non-functional without this uncommitted diff — `boot_memory.py`/`boot_glossary_cache.py` were deleted with 5 importers left broken (`ModuleNotFoundError` reproduced live against a HEAD-only sandbox); this diff is the repair, not a new break. Also found: a dangling docstring cross-reference (`boot_git_checks.py:56`, "Intentionally NOT `_looks_like_git_option` below" — that function was removed by this same diff, zero remaining references repo-wide) and the headline finding: ran the actual relevant test suite (10 files) and got **41 failed, 122 passed** — `DEUDA.md` #9 tracks only 1 of the 41 (the `render_boot_banner_lines` 10-vs-9-arg mismatch, confirmed exact at `test_boot_output.py:1052`); the other ~39 (across `test_boot_output.py`, `test_boot_pending_next_cutoff.py`, `test_boot_tombstones.py`, `test_regression_memory_correctness.py`, `test_migrate_statusline.py`) assert against RESUME/DECISIONS/MEMOS/REMEMBER/GC/TIMELINE and are untracked anywhere — a real, partial test cleanup DID happen this round (9 test files legitimately `D`-deleted per `git status`) but stopped short of these five. See [anti-patterns.md](anti-patterns.md) "Guardián retirado que también protegía superficie viva — cuarta vez" + "Comentario de intentionally NOT X queda colgando" + "Cortar producción sin borrar/migrar los tests" entries.
- 2026-08-02 feat/memoria-v2 §5.3 authorization review (5 files: `lib/constants.py`, `lib/parsing.py` (-74L), `lib/git_helpers.py` (-88L), `lib/managed_blocks.py` (-24L), `lib/cache_sync_check.py`, against the 10 governing docs incl. `PLAN-CONSTRUCCION.md` §5.3, `DEUDA.md`, `DRIFT.md`): NOT MERGEABLE — 1 NO AUTORIZADO+PÉRDIDA COLATERAL (`managed_blocks.py`: `unmassk-toolkit` block deleted whole instead of rewritten per paso 7.12; DEUDA #1 confirmed independently — 6 named consumers still gate on `"BEGIN unmassk-toolkit"` and will never find it again; ran the actual suite: **72 tests fail**, not DEUDA's claimed "doce" — DEUDA's own verification count is stale/wrong by 6x) + 1 NO AUTORIZADO-but-disclosed (`cache_sync_check.py` touched despite being in §5.2's "se queda entero" list; the `"agents"` addition is pre-cleared by DEUDA #5 + recalled project memory dated 2026-08-02, but DEUDA #5's real gap — `_dir_fingerprint` non-recursive, `lib/memory/`/`bin/memory/` invisible — remains unfixed). `constants.py`/`parsing.py`/`git_helpers.py` splits verified SAFE and correctly scoped against §5.3's exact column mapping: `VALID_KEYS` left untouched but proven to have zero production readers today (only `parse_trailers`/`parse_trailers_full`, both dead outside tests); `scan_trailers_memory`/`normalize` removal confirmed dead-code-safe (sole caller `recall.py` already gone from HEAD; the one test using it swapped correctly to `parse_trailers_full()`); mina 2 (`sanitize_trailer_value`) correctly left untouched, its 5 non-memory consumers confirmed intact. Also confirmed the DEUDA #15 silent-overwrite mechanism (`upsert_managed_blocks()` `pattern.sub()` with no diff-check, no warning) is unchanged and still live for the 3 surviving blocks. See [anti-patterns.md](anti-patterns.md) "feat/memoria-v2 — lib/constants.py, lib/parsing.py, lib/git_helpers.py, lib/managed_blocks.py, lib/cache_sync_check.py" entry.
- 2026-08-02 feat/memoria-v2 skills+agents+root periphery review (13 files: `unmassk-core`/`unmassk-close-session`/`unmassk-flow`/`unmassk-standards` SKILL.md, all 9 `agents/*.md`, `README.md`, `.claude-plugin/marketplace.json`, `unmassk-toolkit/.claude-plugin/plugin.json`, against `spec-sistema-memoria-v2.md` + `PLAN-CONSTRUCCION.md` FASE 7 + `DRIFT.md`): NOT MERGEABLE — 2 NO AUTORIZADO + 8 PROMESA FALSA. Gitto retirement (paso 7.7) verified clean across all 9 agent files + `deprecated/gitto.md` (314L, matches plan) + zero `grep -rniI gitto` hits repo-wide — correctly executed. But `unmassk-close-session/SKILL.md` paso 7.10 was executed HALF (removed the v1 flush/curator/resume steps) without its bundled other half (gain the 4 new lines) — the plan authorizes paso 7.10 as ONE step with ONE verification ("un cierre real ejecuta los cuatro"), not a split execution; net result is the skill now does LITERALLY NOTHING toward its own stated purpose ("so nothing decided is lost"), worse than the already-known `DEUDA.md` point 3. New PROMESA FALSA instances found beyond the two given as calibration: `README.md:56,74,79,80` (memory/calibration/close-session claims describing deleted mechanisms — `CALIBRATION.md` confirmed absent via `find`, `unmassk-gitmemory` skill dir confirmed absent via `ls`), `.claude-plugin/marketplace.json:10` ("memory calibration"), `unmassk-core/SKILL.md:83` (dangling `unmassk-gitmemory → Active Hooks` ref, survived the "seis puntos" edit untouched), `unmassk-flow/SKILL.md` (DRIFT counted 20 old-system mentions, diff touched only 2 — Gitto table row + one squash-owner line — leaving ~13-15 incl. retired `Conflict:`/`Resolution:` trailers at line 329 that spec §3.3 explicitly kills). See [anti-patterns.md](anti-patterns.md) "FASE 7 periphery edit executed while its dependency capa is still unbuilt" entry.
- 2026-08-02 feat/memoria-v2 surgery review (4 files: `bin/git-memory-commit.py`, `bin/git-memory-doctor.py`, `bin/git-memory-install.py`, `bin/gitmem`, against `PLAN-CONSTRUCCION.md` §5.3): NOT MERGEABLE — 1 finding NO AUTORIZADO (`git-memory-install.py` had its `managed_blocks.BLOCKS` import + `MANAGED_BLOCK_*` aliases removed even though §5.2 lists this file as "se queda entero"; zero live consumers today since `git-memory-upgrade.py` no longer exists on disk, but plan boundary was crossed without being declared as a new §5.3 row) + 1 PÉRDIDA COLATERAL (`_check_trailer_content`/`_validate_trailer_content` fully removed from `git-memory-commit.py`; §5.3 only authorizes losing "categorías", but the same function also enforced non-empty Memo/Remember description post-sanitize — the exact 2026-07-25 fix — and v1 keeps writing that shape live during v1↔v2 coexistence, confirmed by a same-branch same-day `Memo: antipattern - ...` commit; zero replacement exists anywhere until the v2 aduana ships and is turned on). Doctor.py (`check_hook_execution`/`check_gc_status` removed) and `gitmem` (Fase 0 path fixes) both clean, exact match to plan. Verified Mina 4 (`bin/release.py` → `git-memory-commit.py` with `--trailer Touched=`) still works today, unaffected by this diff — confirms `DRIFT.md`'s own claim. See [anti-patterns.md](anti-patterns.md) "guardián retirado que también protegía superficie viva, tercera vez" entry.
- 2026-08-02 feat/memoria-v2 test-corpus review (13 deleted + 4 modified files under `unmassk-toolkit/tests/`, against all 10 governing docs — spec/PLAN §9.3+§5.1-5.3/PIEZAS §3.1/ARQUITECTURA/TEXTOS/TRAZABILIDAD/CALENDARIO/TESTIGO/DRIFT/DEUDA.md): NOT MERGEABLE as-is — 12/13 deletions BORRADO CORRECTO, 1 MIXTO (`test_boot_freshness_regression.py`: its `TestPullDirectiveGapForUnrelatedUpstream` xfail-pinned class, the ONLY test of `DEUDA.md` #6's exact live bug — unrelated-upstream repos get a bogus `git pull` directive + foreign branch listing — was deleted rather than migrated, even though two new salvage files in the same diff, `test_boot_git_checks.py` + `test_read_retry_contract.py`, correctly preserved everything else surviving from the 3 retired freshness files). Also flagged (not a wrong deletion, but a live regression the batch surfaces): `test_wrapper_trailer_content_validation_contract.py`'s target (`_validate_trailer_content()`) is fully gone from `git-memory-commit.py`, not just buggy — during v1's ongoing write window this reopens the exact "invalid Memo:/Remember: content commits successfully" hole with zero DEUDA.md tracking. Corrected my own same-day memory entry: `parse_trailers_full()` (claimed "the live equivalent" in two modified test docstrings AND in my own prior note) has zero production callers — verified false via full-repo grep, not inherited from confirming `scan_trailers_memory`'s death. Full write-up: [anti-patterns.md](anti-patterns.md) "feat/memoria-v2 — 13 deleted test files + 4 modified" entry.
- 2026-08-02 feat/memoria-v2 capa-5 §12bis paso 3 review (11 scripts `bin/memory/*.py` + `bin/gitmem` + `lib/memory/bench.py`, against PIEZAS.md §0/§2/§10/§10.1/§14, written by 4 non-communicating agents): 0 T1, 4 real duplicates (byte-identical 363B bootstrap header + near-identical `__main__` trailer, both copy-pasted across all 10 scripts; `reindex.py::_rebuild()` reimplements `health.coherence()`'s set-difference reconciliation instead of living in `lib/memory/indexes.py`; `note.py::_corrected_key_pairs()` reimplements `validator.normalize_keys()`'s `MARKER_KEYS` lookup), 1 stale-prose finding (`emojis.py:31-35/57-59` still says `rules.py`/its `CHANNEL_EMOJI["rule"]` producer "no existe todavía" when `rules.py::add()` has been live and wired since earlier the same day — 3rd stale-docstring instance found today), 1 self-check gap in the adversarial bench itself (`bench.py::_attack_5` never verifies the "ofrece parecidas" half of PIEZAS §14 row 5's acceptance bar, despite the module's own docstring promising it checks "la condición completa" for every row — every other 9/10 attacks do check their full row), 1 disclosed business-logic-in-script (`work.py`'s 30-line protected-branch guard, self-acknowledged as a same-day implemented decision, not a bug), 2 `[ASUNCION]`-tagged defaults worth asking the owner (`rule.py`'s `--kind` default "user", `work.py`'s hardcoded `{"main","master"}` branch list). Both of today's earlier disclosed "apaños" (zones.py mkdir, boot.py chdir) independently re-verified as genuinely fixed. Full write-up: [anti-patterns.md](anti-patterns.md) "feat/memoria-v2 — capa 5, los once scripts + bin/gitmem + lib/memory/bench.py" entry.
- 2026-07-08 CI-fix round (issue #52/#54, run 28922061708) LGTM: `%at` unification (boot_git_checks.py) + opt-in `log_stderr_on_failure` (git_helpers.py) + 140-site test encoding='utf-8' sweep. See [false-positives.md](false-positives.md) narrow-signature-run_git-fake entry.
- 2026-07-08 issue #55 `%at` unification round 2 (gc.py + doctor.py `parse_date()` + bootstrap_commits.py) LGTM 2 nitpicks, 0 blockers, 6/6 contract tests green. See [false-positives.md](false-positives.md) test_doctor_after_install `-k`-filter flake entry (pre-existing, not caused by this diff).
- 2026-07-08 issue #55 adversarial-fix round (Argus SEC-LOW-001/002 + Moriarty BUG-4/5/3, diff 0dd396d..HEAD) LGTM 0 issues, 1 suggestion, 2 nitpicks. `lib/date_parsing.py` extracted (centralizes the 2 duplicates, closes prior Yoda Architecture gap). `check_gc_status()` 3-tuple→4-tuple, single call site (`run_doctor`), unpacking correct. 22/22 new contract tests green + 84/84 combined suite green.
- 2026-07-08 issue #55 closing round (diff 7fe8f9f..HEAD): 3/3 prior findings RESOLVED (isascii guard, comment cleanup, docstring). LGTM 1 suggestion (time_ago length-guard asymmetry), 0 nitpicks. See [anti-patterns.md](anti-patterns.md) "mirror functions drift" entry.
- 2026-07-09 issue #53 F6 + SEC-HIGH-001 round (diff dd53977..5c044fa): `reject_hardlinks` opt-in param on both `open_no_follow_symlink()` twins, 6 protected call sites, twin-parity verified byte-identical, 0 issues, 4 suggestions (docstring/comment drift on the 3 SEC-HIGH-001 sites, untested append-mode path), 2 nitpicks. 153/153 relevant tests green. See [anti-patterns.md](anti-patterns.md) "follow-up security fix skips central docstring" entry.
- 2026-07-10 issue #59 A2 nonce round (`d987f94`, feed852/79fdf9a): NOT MERGEABLE as-is — nonce in `user-prompt-memory-check.py` sits adjacent to `<memory-data>`, not bound to it (`</memory-data>` un-nonced), defeating the "infalsifiable frame" goal that A2 was chosen for; sibling fix in `precompact-snapshot.py` does it correctly (nonce on same line as both header+footer). Bytes-transport (`\r`→`\n`) + ReDoS cap + LOW-17 all correct, 167/167 green. See [anti-patterns.md](anti-patterns.md) "A2 token-fence nonce placed adjacent to the marker" entry.
- 2026-07-10 issue #60 rate_limited relabel round (`ac3e805..HEAD`, wips 3be6552/d630e14, decision ceef426): NOT MERGEABLE as-is — `lib/boot_git_checks.py:791-792` new claim ("FETCH_HEAD<300s = confirmed fresh") directly contradicts untouched SEC-LOW-001 comment at `:449-459` ("freshness claims never come from this timestamp"); relabel upgrades a spoofable local mtime into a confident "remote (synced)" claim. Flag for Argus re-confirmation. Gate math, failure-state coverage, §34 contract, subprocess round-trip all clean; 96/96 green (incl. 2 new boundary tests added mid-review, 299s/301s edge). See [anti-patterns.md](anti-patterns.md) "Relabeling a rate-limit-derived stamp" entry.
- 2026-07-10 issue #60 v2 re-review (`9691b64..HEAD`, decision `90d096d`, wips 3f8122c/eb3e554): LGTM 4 suggestions, 2 nitpicks, 0 issues. Moriarty's v1 T1 (FETCH_HEAD mtime lies) fixed for real via own success stamp (`.claude/.unmassk/boot-fetch-stamp.json`, mkstemp+os.replace atomic write, symlink+hardlink-safe read, remote/branch identity match). SEC-LOW-001 comment now fully truthful (verified via grep — zero FETCH_HEAD reads left in code). 9/9 Ultron test re-bases verified one-by-one, semantics preserved; Dante's 4-test v2 contract confirmed untouched by Ultron's commit (`git diff 3f8122c..eb3e554`). Fail-open manually reproduced (corrupt JSON/wrong-shape/symlinked stamp → None in all 3, matches docstring). Real gap found: `_check_remote_is_live()` (`lib/boot_git_checks.py:834-847`) hardcodes `age_seconds=None` and its docstring falsely claims remote identity is unknown at that point — it IS known (remote_name is a param, remote_branch available to caller but not forwarded) — degrades message fidelity (shows "never synced" instead of "last fetch Xs ago, unverified" for a remote that died after a real prior sync) but never re-opens the false-"synced" vector. File also crossed 1000 LOC (841→1041 in this round alone) — flagged for a `lib/boot_fetch_stamp.py` split mirroring the `boot_glossary_cache.py` precedent. 141/141 green.
- 2026-07-10 issue #60 v3 round 3 (`eb3e554..HEAD`, decision `787b698`, wips `043a242`/`df1bb4f`): LGTM 0 issues, 0 suggestions, 0 nitpicks — closing round, all 6 prior findings (4 suggestions + 2 nitpicks) verified RESOLVED one-by-one with evidence: S1 age-fidelity in `_check_remote_is_live()`'s no_remote branch via new `_read_stamp_age_by_alias_only()` (confined to that one caller, docstring explicit about why it's safe there); S2 module split → `lib/boot_fetch_stamp.py` (357 LOC, `boot_git_checks.py` 1041→907), re-imported by original name, zero duplicated logic left behind, single call site for every re-exported name (grep-verified), no circular import; S3 → `TestReadOwnStampAgeDirectCalls` (4 pinning tests, all green for the right reason — corrupt-JSON/wrong-shape fail in `_load_own_stamp` before identity compare, symlink/hardlink fail in the symlink-safe open, so the 3-arg legacy call signature with `remote_url=None` never actually reaches the identity comparison, matching the docstring's own claim); S4 → seeding assert (`os.path.isfile(stamp_path)`) added to `test_fresh_fetch_head_skips_fetch`; N1 → chmod-Windows comment rewritten factually; N2 → `schema_version` bumped 1→2 with strict-mismatch-collapses-to-absent, tested by `test_stamp_with_unknown_schema_version_is_treated_as_absent`. New T1 fix (identity by remote URL, decision 787b698, closing Moriarty's v2 cross-repo stamp-copy PoC): `remote_url` threaded `_check_remote_is_live→_resolve_fetch_target→gate→_write_own_stamp`, `_write_own_stamp` no-ops on falsy URL, `_read_own_stamp_age` compares remote+branch+URL+schema strictly. Ultron's 2 non-contract test re-bases (`git remote set-url`→`shutil.rmtree(bare)` in `TestRateLimitedStampSurvivesRemoteBreakage` and `test_fetch_failure_returns_failed_with_prior_age`) verified to preserve the exact failure semantics they were built to inject (get-url returns same string, fetch still fails — traced `bare`/`_add_bare_remote` return values to confirm no accidental behavior change). Dante's 2 new RED-v3 tests (`TestOwnStampIdentityIncludesRemoteURL`) confirmed byte-identical between Dante's wip and Ultron's wip (`git diff 043a242..df1bb4f -- tests/`) — untouched, green for the right reason. 139/139 relevant + 1244/1244 full suite green, exit 0.
- 2026-07-10 issue #60 v4 mini-review (`df1bb4f..154a80d`, wips `32379b7`/`154a80d`, decision `174d82b`): NOT MERGEABLE as-is — 1 issue. Production guard (`_check_remote_is_live()`, `lib/boot_git_checks.py:725`, `url == remote_name` → treated as unresolved) is correct, sole call site, docstrings accurate, 141/141 green. But `test_stamp_claiming_alias_as_url_is_never_trusted_by_an_unrelated_repo` and its surrounding docstrings falsely claim independent "read-side" coverage — empirically proven redundant with the write-side guard (test still passes with the v3 `remote_url` comparison in `_read_own_stamp_age()` deleted, confirmed by patching and re-running: `2 passed`). Write-side pinning test is solid and correctly isolated. See [anti-patterns.md](anti-patterns.md) "read-side test silently redundant with upstream write-side guard" entry.
- 2026-07-11 issue #61 CI-flake-hardening round (`ee64aa2..HEAD`, wips `07e194a` Ultron / `dd45b2f` Dante): LGTM 0 issues, 1 suggestion, 1 nitpick. Ultron's 9-site stderr breadcrumbs (`log_stderr_on_failure=True` on 7 sites + manual `print()` on boot_memory.py's 2 sites, deliberately avoiding the kwarg there) verified zero-behavior-change via `run_git()` source read (kwarg only prints, never touches returncode/stdout) — matches false-positives.md narrow-signature-run_git-fake entry, now with a concrete example of when the kwarg genuinely WOULD break 4 other test files (avoided correctly). Dante's anti-vacuity retries (needle-must-match, not truthy) + 6→1 `_load_git_helpers_bound_to_repo(**kwargs)` consolidation verified correct (grep confirmed zero remaining narrow-signature doubles reach the changed sites). Windows encoding verified safe (all consumer hooks already call `force_utf8_streams()`). Fixture volumes (510/300) intact. 84/84 relevant + 1246 passed/2 skipped/0 failed full suite, matches Dante's own reported numbers exactly. Real gap found: no test asserts the 9 new breadcrumbs actually fire on a simulated git failure — see [anti-patterns.md](anti-patterns.md) "Observability-only breadcrumbs added to production code with no test asserting they actually fire" entry.
- 2026-07-11 issue #63 boot-simplification round (`884bc2b..25ef9fa`): NOT MERGEABLE as-is — 1 issue (dangling memory link, Dante's `issue-63-boot-simplification-contract-notes.md` untracked), 0 suggestions, 1 nitpick. P1 manifest-gate fail-open verified correct + P1↔P2 coherence verified via `install_apply.py`'s action order (CLAUDE.md write before manifest stamp → regen fires exactly once). P2 clean removal (zero residual per-message evaluation), P3 `_is_real_repo_source()` handles worktrees/submodules correctly, P4 dedup confirmed single-home. 206/206 focused + 1265 passed/2 skipped full suite green. See [anti-patterns.md](anti-patterns.md) recurrence note under "Commit touches an agent-memory MEMORY.md index without the topic file it points to".
- 2026-07-14 unmassk-3d commit-review (validate_mesh/setup_cad_env/run_cadquery.py, no diff yet on main): NOT MERGEABLE as-is — 1 issue (stale-output false success in `run_cadquery.py`, reproduced live), 3 suggestions, 2 nitpicks. See [anti-patterns.md](anti-patterns.md) "Run script, then check output file exists" iterate loops trust stale artifacts.
- 2026-07-13 unmassk-pentesting 30-skill auto-trigger re-audit (post-disambiguation): all 6 named seams RESOLVED with explicit clauses; format 30/30 clean (no skill names another). Full-pass grep found 3 NEW unresolved trigger collisions the rewrite missed: pass-the-hash (authentication↔system), container escape (system↔cloud-containers), cache poisoning (server-side↔web-app-logic) + soft bare-"privilege escalation" gap (web-app-logic). 27/30 fire clean, 3 need a follow-up disambiguation pass. See [anti-patterns.md](anti-patterns.md) "Skill-description disambiguation pass fixes the named seams but misses identical trigger phrases outside the reviewed seam list".
- 2026-07-15 unmassk-electronics sensor_gate.py + §34.6 standards.md commit-review (pre-commit, working tree): LGTM 0 issues, 2 suggestions, 1 nitpick. Tolerance-band math, median filter, no-sensor path, never-crash CLI all verified correct; 69/69 tests green. §34.6 paragraph correct/self-consistent but cites nonexistent "§60" — see [anti-patterns.md](anti-patterns.md). NaN-CLI degrade-to-commanded_unverified confirmed acceptable — see [false-positives.md](false-positives.md).
- 2026-07-15 Next-cutoff-by-last-context commit-review (`045ed0b..9e24d29`, `lib/boot_memory.py::extract_memory()`): LGTM 0 issues, 3 suggestions, 2 nitpicks. `last_context_ts` is a true order-independent MAX (verified by code + `TestPendingNextCutoffTwoContexts`), `>=` tie-break matches contract (`TestPendingNextCutoffTimestampTie`), fail-open confirmed (no crash path, `pending`'s dict keys always present). Cap-before-filter edge (point 5) judged NOT a live bug — see [anti-patterns.md](anti-patterns.md) "Two independently-named size/depth constants coincidentally equal" entry. 6/6 contract + 140/140 relevant regression green.
- 2026-07-14 unmassk-electronics 3-script commit-review (serial_verify.py + setup_micro_env.py + setup_pi_env.py, no diff yet on main): NOT MERGEABLE as-is — 2 issues (PEP-668 asymmetry between the two setup scripts; ssh call missing BatchMode/ConnectTimeout, both concrete "breaks itself on first real use" bugs, not attacker-hardening), 6 suggestions, 2 nitpicks. serial_verify.py's evaluate_lines() gate logic verified correct (6/6 Dante tests reproduced green, module imports without pyserial installed as claimed). setup_micro_env.py confirmed ~90% byte-identical to unmassk-3d's already-reviewed setup_cad_env.py (diffed directly) — 2 occurrences, below the 3+ duplication threshold, flagged as suggestion only. See [anti-patterns.md](anti-patterns.md) "PEP 668 handled on dev-box pip install but skipped on sibling SSH-remote pip install" and "subprocess.run(['ssh',...]) with no BatchMode/ConnectTimeout can silently block on first-connection host-key prompt".
- 2026-07-18 issue #72 "adelgazamiento" anti-attacker-test-removal commit-review (`9d43382..HEAD`, plan `docs/plan/refactor-adelgazamiento-72.md`): NOT MERGEABLE as-is — 2 issues, 2 suggestions, 1 nitpick. `sanitize_trailer_value()`'s exotic control-byte class (\x1b/\x7f/\x1c-\x1f/\x85) + `<memory-data>` fence regex lost ALL direct unit coverage despite being live at 15+ production sites; `TestScopesInjectionSanitization` deleted despite its own docstring naming "corrupted Bilbo run" (non-attacker trigger), violating the plan's own ambiguity-stop rule. Code edit (`OLD_BIN_FILES` 2-line fix) verified clean/single-source-of-truth; 4 fully-deleted files + 6 mixed-file excisions verified with zero orphaned imports/fixtures. See [anti-patterns.md](anti-patterns.md) "cut attacker-defense tests leaves canonical sanitizer at zero coverage", "plan's own stop-and-report rule not honored", "stale by-name test references in production comments" entries; [false-positives.md](false-positives.md) TOCTOU fd-leak entry (almost flagged, verified covered by a differently-named surviving test).
- 2026-07-19 fix-atomic-claude-md-write commit-review (`f7945f3..HEAD`, plan `docs/plan/fix-atomic-claude-md-write.md`, House T1 diagnosis): NOT MERGEABLE as-is — 1 issue (confirmed live: `lib/git_helpers.py::_AtomicWriteNoFollowSymlink` silently narrows an existing CLAUDE.md's permissions from e.g. `0o644`→`0o600` on every managed-block update, since `os.replace()` swaps in `tempfile.mkstemp()`'s fixed-mode temp file instead of preserving the original's mode — the old non-atomic `open_no_follow_symlink(path,"w")` correctly preserved existing-file permissions; git doesn't track full POSIX mode so this never shows in `git diff`), 2 suggestions, 2 nitpicks. Coverage of all 4 write sites confirmed complete via grep (zero un-routed `claude_md` "w" opens remain). User's top concern (point 2, parent-dir symlink via `.claude`-style intermediate) verified NOT applicable/NOT a regression — CLAUDE.md's parent is always `target` (the project root itself, never a nested symlinkable dir), and no `verify_path_within_project` call ever existed on any of the 3 writers before or after this diff. Real exception mid-`with` correctly triggers cleanup (verified live, matches docstring) — but the shipped Test 1 bypasses the real object's own `__exit__` via a proxy wrapper, so that cleanup path is only manually verified, not test-covered. `atomic=False` default confirmed zero-regression for the ~45 other call sites (117 relevant tests green). See [anti-patterns.md](anti-patterns.md) "tempfile.mkstemp() + os.replace() atomic write silently narrows the destination's permission bits" entry.
- 2026-07-25 unmassk-toolkit "dead-end memory loop" commit-review (`c9939f3..HEAD`, new `deadend` Memo category + Bilbo whitelisted in `pre-task-recall.py`): NOT MERGEABLE as-is — 2 issues, 3 suggestions, 1 nitpick. Confirmed a real producer→consumer format gap: bilbo.md/gitmemory SKILL.md promise the multi-line per-symbol `DEAD-ENDS` block is "persisted verbatim", but `Memo:` trailers are single-physical-line only (traced through `parse_trailers`, both validate hooks' `.split(" - ",1)`, `git-memory-commit.py`'s line-per-trailer build, and `recall.py::_format_block` which never reads commit body text) — zero round-trip test for the real multi-symbol shape, only a single-line stand-in. Also confirmed Ultron's suspicion: `pre-validate-commit-trailers.py:122`'s error literal still omits `stack` after adding `deadend` (2nd time this exact string has drifted from `MEMO_CATEGORIES`). `post-validate-commit-trailers.py` confirmed clean (imports the constant, no hardcoded list). Dante's test reconciliation in `test_pre_task_recall.py` verified honest — gitto-exclusion coverage moved to gitto-specific tests, not deleted. 67/67 green. See [anti-patterns.md](anti-patterns.md) "Docs promise persisted verbatim for a multi-line block" entry.
- 2026-07-15 boot BRANCHES-section + fetch-all-branches commit-review (`d3aea09..HEAD`, wips e32f30d/17277af, no test files touched in either): NOT MERGEABLE as-is — 1 issue (confirmed live via 2-repo repro: missing `--prune` on the new `+refs/heads/*:refs/remotes/<remote>/*` refspec leaves deleted-upstream branches permanently visible in the new BRANCHES section even after a fully successful fetch — no `--prune` anywhere in the codebase), 4 suggestions (zero test coverage for ~150 new LOC incl. the refspec itself, dead HEAD-exclusion equality check, FETCH_TIMEOUT_SECONDS=10 tuned for one branch now fetching all, BRANCHES section has no cross-reference to fetch_state/staleness), 0 nitpicks. Everything else verified sound: fetch hardening (env/timeout/`--no-tags`/`--` separator) fully intact, refspec correctly scoped to the one resolved remote (verified live, never touches other remotes), stamp-truthfulness reasoning (keyed to remote+branch+URL, the tracked branch is always included in the wider fetch) confirmed correct, `remote_name=None` nulling-before-derivation guard against an unrelated upstream traced line-by-line and correct (no second exclude_remote param needed, unlike get_timeline()/extract_glossary() which scan `--all`), fail-open of `get_remote_branches()`/`render_branches_section()` confirmed (for-each-ref failure or empty output → `[]`, never raises), cap+"(N more)" notice logic correct (verified with a live >20-branch-shaped trace), section insertion causes no ordering regression elsewhere. See [anti-patterns.md](anti-patterns.md) "Wildcard fetch refspec without --prune" and "refname:short of a remote's symbolic HEAD ref collapses to the bare remote name" entries.
- 2026-07-25 trailer-content-validation wrapper round (`c36f927..e2dafbe`, wip `e2dafbe`): NOT MERGEABLE as-is — 1 issue (confirmed live: `_validate_trailer_content()` checks the non-sanitized raw value; a control-byte-only description passes the non-empty check but collapses to empty after downstream `sanitize_trailer_value()`, producing a committed `Memo: preference -` with no description — same silent-corruption class the feature exists to close), 2 suggestions (hook's Remember error message still hardcodes `user|claude` instead of deriving from `REMEMBER_CATEGORIES` like the Memo block does; test file's local `REMEMBER_CATEGORIES = ("user","claude")` tuple should now import the real constant instead of a hand-typed duplicate), 0 nitpicks. Fail-closed ordering, enum scoping (only Memo/Remember validated, Decision/Why/Touched/Next untouched), first-`" - "`-split correctness for descriptions containing extra hyphens, and REMEMBER_CATEGORIES single-source-of-truth promotion all verified correct. 25/25 relevant tests green. See [anti-patterns.md](anti-patterns.md) "Trailer content validation runs on the pre-sanitize value" entry.
- 2026-08-02 feat/memoria-v2 capa-2/3 round (`lib/memory/{gitcmd,ids,indexes,rejection,validator,notes,query}.py` vs `docs/memoria-v2/PIEZAS.md` §7.1-§8.2, no commit yet, working tree): NOT MERGEABLE as-is — 4 findings, all coverage/documentation gaps, 0 confirmed functional bugs (34/34 relevant tests green). `gitcmd.commit()`'s empty-paths `ValueError` and `repo_root()`'s outside-repo `RuntimeError` have zero direct test (only happy-path via `notes.write()`). `indexes.archive()` fully implemented, zero production callers (only declared consumer `notes.replace()`/`close()` deliberately `NotImplementedError` in this same round) and zero test invocation — manually verified live it round-trips correctly today, but nothing pins it. `indexes.remove()`'s own guard clauses (ValueError/FileNotFoundError) untested in isolation (only exercised via the happy/rollback path). `ids.py` module docstring stale — claims `notes.write` has no real consumer yet, but `notes.py` exists in the same branch and calls `ids.next_id()`. Frontier (stdlib+siblings only), "una sola puerta" (validator/query as sole implementations), and each module's "qué NO hace" all verified clean — no boundary violations found. See [anti-patterns.md](anti-patterns.md) "memoria-v2 lib/memory/ capa-2/3 review" entry.
- 2026-08-02 feat/memoria-v2 hooks round (4 files: `hooks/hooks.json`, `hooks/stop-dod-check.py`, `hooks/user-prompt-memory-check.py`, `hooks/pre-validate-commit-trailers.py`, against `PLAN-CONSTRUCCION.md` §5.3 + `DEUDA.md` + `DRIFT.md` §3, working tree): NOT MERGEABLE as-is — 0 NO AUTORIZADO chunks, 2 PÉRDIDA COLATERAL (both confirmed live), 1 still-open mina, 25 currently-failing tests found (2 directly caused by this diff, 23 pre-existing/other-scope). (1) `stop-dod-check.py`: `grep -c "# Check"` = 2, not 3 — Check 3 (`get_last_commit_next`, unresolved `Next:` warning) died alongside the two authorized-to-remove functions, exact match to `DEUDA.md` #4, still unfixed. (2) `user-prompt-memory-check.py`: banner text removal itself matches §5.3 exactly, but the removed `_BANNER` was also the sole carrier of two untested side-effects — `TestUserPromptMemoryCheckCp1252` (asserts `"git-memory-recall.py"` substring, now absent everywhere) and `TestNoRegression::test_base_output_not_empty` ("INVARIANT — must not regress", now empty stdout reachable) both FAIL live (`pytest` run, reproduced). Same file's untouched-by-this-diff install/boot instructions still say `skill="unmassk-gitmemory"` + read a `CALIBRATION.md` that don't exist (skill dir deleted at `177420b`, 25 commits before HEAD) — proven live-reachable via 8 failing `test_user_prompt_skill_router.py` tests on `lib/skill_router.py`'s `SKILL_TRIGGER_PHRASES["unmassk-gitmemory"]` table, deferred to paso 7.12 but not yet done. (3) `hooks.json`: all 3 removed hook entries (`pre-memory-dedup-gate.py`, `pre-task-recall.py` Task matcher, `precompact-snapshot.py` PreCompact) correctly match §5.1's "se retiran enteros" list and FASE 9 paso 9.1 — cross-check confirmed no declared-hook-missing-file and no live-file-undeclared remain; historical note (not a current defect): the 3 underlying `.py` files were deleted at `177420b` while `hooks.json` kept declaring them for 25 commits until this exact diff — a real "hook that would crash on every fire" window that this diff itself closes. (4) `pre-validate-commit-trailers.py`: only a stale-comment cleanup (dropped `agents/gitto.md:239` cross-ref after gitto's move to `deprecated/`); `CLAUDECODE` (no underscore) env-var fix is CONFIRMED ALREADY LIVE in both HEAD and working tree (dated 2026-07-29 per its own comment, predates this branch's hook work) — the historically-known bug is closed, do not re-flag. Mina 1 (paso 2.8, adding v2 wrapper paths to `uses_wrapper` on line 51) is CONFIRMED STILL OPEN — only recognizes `git-memory-commit.py`/`git-memory-log.py`, zero v2 paths added — not urgent yet (no v2 script commits anything until Capa 5 exists) but flagged since §5.3's own FASE 2 table places it alongside gitcmd/notes/etc., already reviewed as done. Separately discovered and reported to the user live during this review: the same file's `\bgit\b.*\bcommit\b` regex (line 53) false-positive-blocks read-only `git show`/`git diff`/`git cat-file` commands whenever the command references any path containing the substring "commit" (e.g. this file's own filename) — blocked my own review commands mid-session; pre-existing, unchanged by this diff, not caused by it. See [anti-patterns.md](anti-patterns.md) "A removed text block silently doubled as a test-satisfying invariant" and "`unmassk-gitmemory` skill-reference rot recurs a third surface" entries.
- 2026-08-03 feat/memoria-v2 peer review of all 9 `agents/*.md`, second pass after fix (working tree, no commit yet): NOT MERGEABLE — both original findings (T1 word-search mechanism, T2 security/antipattern word-search) verified genuinely CLOSED via a real end-to-end round-trip in an isolated scratch repo (real commits, real notes) plus live execution in this repo. But 1 new T1 found live: the two-hop `GITMEM=` resolution line added to all 9 files is completely silent (zero output) when both hops fail to find `bin/gitmem` — confirmed today's normal case for any project other than this repo, since `bin/gitmem`/`bin/memory/` are uncommitted/untracked and no installed plugin cache has them; the silent branch is a permanent design flaw, not just today's unreleased state. Plus 2 T2: `search.py --file <path>` (the abandoned original fix) remains live, undocumented-as-broken, structurally always-false for real notes — not used by these 9 files but a live landmine; "Memory consulted" report line present at the right location in all 9 but ungated everywhere, sharpest in Ultron's own Exit Gate which has zero checkbox for it despite Ultron's Boot Step 5 mandating the search. See [anti-patterns.md](anti-patterns.md) "B20 zone memory, second pass" entry.
- 2026-08-04 feat/memoria-v2 P5 incident-close-question round (4 files: `lib/memory/validator.py::validate_incident_close_question`, `bin/memory/remove.py` `--restriction` no-longer-required + new `main()` branch, `tests/memory/test_remove_incident_close_question.py`, `tests/memory/test_rejection_relaunch_commands.py`, working tree, no commit yet): NOT MERGEABLE — 1 T1 confirmed LIVE (`remove.py::main()`'s new branch silently skips the mandated P5 question when `query.by_id(args.id)` returns `None` — reproduced with a desynced index (`INCIDENTS.md` id renamed, git log still has the old id): `remove.py <desynced-id> "..."` exits 0, `"✅ ... archivada"`, P5 never asked, no warning at all; sibling guard `_guard_restriction_new()` three functions above already handles this exact `None` case with a loud stderr rejection and wasn't reused). Simpler case (id missing everywhere) at least fails loud but with a generic message that never mentions the wall question (`remove.py: 'I-999' no esta en INCIDENTS.md`, RC=1) — verified live. Plus 1 T2 (`test_rejection_relaunch_commands.py:8,534-535,562-567` — docstring/retired-test-comment still claims `bin/memory/remove.py:53`'s `--restriction` is `required=True`; false as of this same diff's point 2, `default=None` now) + 2 T3 (same file's `test_extraction_finds_a_realistic_number_of_relaunch_commands` docstring count "8 en validator.py" stale by +2, real count now 10, comment-only, doesn't affect the `>=15` assert; and a PRE-EXISTING — not new today, confirmed via grep across all of `validator*.py` — gap where `rejection.py::_render()` always emits generic `"⛔ {what}"` / `"Relanza:"` instead of each TEXTOS.md §1 template's own category prefix/contextual phrasing, which `validate_incident_close_question` inherits rather than fixes). Everything else verified clean: TEXTOS.md §1.10 molde matched line-for-line (id header, `[zone1][zone2]`, causa line, sí/no options, both relaunch commands byte-for-byte via the AST-scan watcher); `--origin` comma→space fix confirmed correct against `note.py:86`'s real `nargs="+"`; M/D close paths correctly ask nothing; both new test files pass (53/53) plus full `tests/memory` suite (373/373). See [anti-patterns.md](anti-patterns.md) "New 'ask before proceeding' branch omits the sibling guard's lookup-failure check" entry.
- 2026-08-03 feat/memoria-v2 peer review of all 9 `unmassk-toolkit/agents/*.md` (B20 "zone memory" boot-step addition, working tree, no commit yet), first pass: NOT MERGEABLE as written — 1 T1 (identical across all 9 files), 1 T2, 0 nitpicks worth surfacing. T1: the added `git log --follow -- <path>` + look-for-`[ID][zone1][zone2]` mechanism cannot work on real source files — traced live through `format.build_subject()`/`notes_commit.py::write_work()`, memory-note commits (bracket-tagged) and code-work commits (touch the real path) are structurally disjoint write paths; verified live against this repo's own `git log --follow -- unmassk-toolkit/agents/cerberus.md`. T2: Cerberus/Argus's "notes keyed security or antipattern" clause isn't deliverable by the single `gitmem search <zona>` command named — `report_render.py`'s memo/incident block renderers never print `Keys:` (only `_restriction_block` does, confirmed against TEXTOS.md's own canonical example); needs a separate word-search. Everything else verified sound: structural insertion (numbering/sections) clean in all 9 files, per-role data assignment (R/D/I/security-antipattern split) matches the task's declared table exactly file-by-file, all 9 real `gitmem` subcommands (note/work/wip/remove/next/search/zones/rezones/rule) confirmed live via direct execution — `gitmem boot` correctly errors as retired, `gitmem search <zona> [--todo]` executes cleanly, no orphaned `Gitto` references left in any of the 9 files, Bilbo's deliberately-minimal-boot exception judged justified (zone-memory step still added, just as prose after the Memory-path paragraph rather than inside the near-empty bash Boot block — consistent with that file's own already-minimal style, not a crack). See [anti-patterns.md](anti-patterns.md) "feat/memoria-v2 — B20 zone memory boot step" entry.

## 2026-08-05 — unmassk-close-session skill, first review (SKILL.md + close-agent-prompt.md + session_transcript.py)

NOT MERGEABLE — 2 issues, 2 suggestions, 1 nitpick.

T1: (a) `session_transcript.py`'s "conversation, and only the conversation, no injected reminders" guarantee (docstring :22-26) is violated by its own named-tag filter (`_INJECTED` :58-71) — `<skill-format>true</skill-format>` is not in the allowlist and its unwrapped trailing skill-doc content leaks through; the untagged `[PROJECT MEMORY — auto-recalled...]` recall block is structurally unreachable by any tag-based filter. Reproduced by running the file's real regex functions against reconstructed live prompt text (this review's own task input contained both shapes verbatim). (b) `_last_close`/`_commits` (:167-169, :190-192) conflate a real `git log` failure with "no boundary/no commits yet," the exact pattern `lib/memory/context.py::latest()`'s own docstring in this codebase declares unacceptable for the identical query.

T2: `<GITMEM>` invoked bare in close-agent-prompt.md (:52,74) instead of `python3 "$GITMEM"` like every other agent prompt in the repo (cerberus/argus/bilbo/alexandria/ultron/yoda/dante/house.md all use the python3-prefixed form) — Windows-parity risk given the script claims cross-platform support. Alexandria's docs step (SKILL.md:32) has no concrete task input spec, unlike the close agent's fully placeholder-driven prompt.

Files under review were being actively edited during the round (mtimes ~60-90s before the final read pass) — SKILL.md shrank from 104 to 63 lines mid-review, close-agent-prompt.md and session_transcript.py also changed (session_transcript.py added rival-transcript-window detection, which correctly resolved a concurrent-session risk I was about to flag independently — do not re-flag that as missing). Findings above are against the final, stable content confirmed via mtime check before writing the review.

## 2026-08-05 — feat/memoria-v2 boot fetch-first round (remote.py, timefmt.py + test_remote.py new; boot.py×2, context.py, gitcmd.py, model.py changed)

NOT MERGEABLE — 1 issue, 3 suggestions, 3 nitpicks. 409/409 `tests/memory` green (does not catch the issue below).

T1: `remote.latest_activity()` (`lib/memory/remote.py:154-180`) doesn't exclude `refs/remotes/<remote>/HEAD` from its `for-each-ref --sort=-committerdate --count=1` query; that symref's `refname:short` collapses to the bare remote name and wins ties against its own target branch (alphabetical tie-break: `"HEAD" < "main"`). Reproduced live with pinned commit dates: standing on a local branch other than the remote's default, with the newest activity on `origin/<default>`, `latest_activity()` returns `branch="origin"` — `boot.py::_remote_block()` then prints `"Last worked on: origin · ..."`, a nonexistent branch name, in exactly the cross-machine scenario the file was built for. Zero test coverage (`test_remote.py` never fetches from a real remote with its `HEAD` symref present). See [anti-patterns.md](anti-patterns.md) "`refname:short` of a remote's symbolic HEAD ref recurs a second time" entry — same root cause as a 2026-07-15 finding in the old `boot_git_checks.py`, this time live instead of dead.

T2: `bin/memory/boot.py:67-71`'s report write reimplements atomic-write (fixed temp filename, no fsync) instead of reusing `gitcmd.atomic_write()` already on its own `sys.path`; zero test coverage of `boot.py`'s new `_remote_block()`/`render()` integration (`test_boot.py` predates the 2026-08-05 remote feature entirely — confirmed via full grep, zero hits for `remote`/`BRANCH` outside `test_remote.py`); `boot.py`'s module docstring collaborator list ("se construye con context, health, indexes, notes, query") is stale, missing `remote`/`repo_guard`/`timefmt`/`emojis`, and `PIEZAS.md` §9.5 wasn't touched either.

T3: `model.py`'s own docstring says "Catorce dataclasses" (14, correct) but titles the enumeration "Las trece clases" (13, stale); `BootSummary.remote: object | None`'s `None` branch has zero producers anywhere (only call site, `boot.py:190`, always passes a real `RemoteState`) — dead defensive surface, undisclosed; `test_remote.py::test_a_repo_with_no_remote_fails_without_hanging` asserts success (`assert ok`) despite its name saying "fails".

Positives confirmed: `test_remote.py` round-trips every assertion against an independent git answer (`rev-list`, `merge-base`, `config --get`), no hand-typed literals; `gitcmd.run()`'s new `env` overlay is additive and its only consumer is live-verified against real git credential-helper behavior; `timefmt.py` genuinely closes a prior `utc_label` duplication (`report_render.py:108` now aliases it); `context.py`'s `--all --date-order` is correctly sequenced after the fetch in `boot.build()`.

## 2026-08-05 — unmassk-close-session skill, second review (full rewrite of all 4 files)

NOT MERGEABLE — 4 issues, 2 suggestions, 0 nitpicks. Task explicitly said prior findings were closed and to review fresh; verified live, not from memory.

**T1(a) from the first round (skill-format/PROJECT MEMORY leak) is NOT fixed** — reran the exact regexes against the module: `"skill-format" in _INJECTED` → `False`; both a reconstructed `<skill-format>true</skill-format>` sample and a `[PROJECT MEMORY — auto-recalled...]` bracket-header sample survive `_INLINE_TAGS.sub()` byte-for-byte unchanged. The docstring's absolute guarantee (`:22-26`) is unchanged too. **T1(b) from the first round (git-log-failure vs no-boundary conflation) IS fixed** — `_git_log()` (`:94-107`) now raises `RuntimeError` on any non-`_EMPTY_REPO` git failure instead of returning `""`.

Two brand-new T1s found this round, both in `close-agent-prompt.md`, both reproduced/traced live: (1) line 29-30 ("'[NEXT]' and an emoji go in front of it") reads as an instruction for the agent to prepend these to the headline text itself, but `format.build_context_message()` (`lib/memory/format.py:388-389,397-402`) already unconditionally prepends the fixed `[NEXT] 🧭` prefix — confirmed live: feeding a headline that already contains `[NEXT] 🚀 ` produces a doubled, malformed commit subject (`[NEXT] 🧭 [NEXT] 🚀 ...`) that SKILL.md's own verification ("starts with `[NEXT]`") would not catch. (2) STEP 3 (`:49-56`) says "write **the body** to a file," reusing STEP 2's exact label for one of three named parts (HEADLINE/BODY/TAIL) — since `next.py`'s only free-text field is `--context`, the TAIL (mandatory commit list) must be concatenated into the same file to ever reach the commit, but nothing says so explicitly; a literal reading drops it silently.

Plus one more T1: the script's rival-transcript-window warning (`_newest_transcript`, `:159-171`, praised as a good addition last round) is embedded in the output file's `warning:` header line, but `close-agent-prompt.md`'s STEP 1 (`:19-25`) only tells the agent to check the `boundary:` line, never `warning:` — the safety mechanism exists but isn't wired into the agent's required checks, the exact "acordarse no es un mecanismo" anti-pattern this project already named (`DEUDA.md:361`).

Two T2s: SKILL.md (`:32-35`) tells the orchestrator to hand Alexandria "the commit that closed the previous session" without specifying how the orchestrator itself determines it — `_last_close()`'s precise subject-prefix algorithm never exposes a raw SHA in the script's own output, only an ISO date; SKILL.md's "Not part of a close" table (`:52-61`) omits the CHANGELOG exception that `alexandria.md:109,165-167` explicitly states ("belongs to `merge`"), even though the task specifically asked to check CHANGELOG-exception consistency.

Also confirmed (avoided as false positive): the auto-recalled project memory `[DECISIONES] (plugin/close-session)` about version-bump/changelog/blocker/decision-flush being close-protocol steps is **stale**, superseded by `DEUDA.md` B34/B42 (both dated 2026-08-04/05, the most current governing decisions) and by this task's own explicit "Deliberate design decisions" list ("the close collects no signals... those are saved when they happen"). Did not flag the absence of wall-pruning/blocker-registration/plan-issue-currency as a regression — see false-positives.md entry.

## 2026-08-08 — `notes_commit.py::write_work()` point-9 fix, round 2 commit-review (`94dd863`)

NOT MERGEABLE — 1 new T1, live-reproduced (not hypothetical), 0 new suggestions/nitpicks. All 3 round-1 findings confirmed genuinely fixed: reset return code now checked and the corrupt commit's SHA is named on failure (`:727-739`), never claims "se deshizo" unless the reset actually returned success; `own_commit_sha` pinned via `git rev-parse HEAD` immediately after `stage_and_commit()` succeeds (`:679-680`) and rechecked against a fresh `HEAD` read before touching history (`:709-724`), correctly refuses to touch history and never says "se deshizo" when the recheck shows HEAD moved; `--no-filters` added to `git hash-object` (`:344-346`); 4 new deterministic tests (`test_reset_failure_leaves_the_corrupt_commit_alive_and_names_it`, `test_reset_success_actually_removes_the_corrupt_commit_from_history`, `test_head_moved_by_another_process_leaves_history_untouched`, `test_matching_content_undoes_nothing`, all in `test_notes.py:2276-2497`) all green, all compare against real `git log`/`rev-parse` output, never against what `WriteResult` claims. **The 4th hole:** the recheck at `:709-712` compares `own_commit_sha` against a freshly-read `HEAD`, but the reset itself (`:726`) still passes the literal `"HEAD~1"`, resolved dynamically by git when the subprocess actually runs — not pinned to `own_commit_sha`'s own parent. Live-reproduced via a standalone script (scratchpad, `gitcmd.run` monkeypatched to inject a real `git commit --allow-empty` at the exact instant `notes_commit.py` calls the reset): the foreign commit is silently discarded by `git reset --mixed HEAD~1`, the corrupt commit ends up surviving as `HEAD` (neither goal achieved — foreign work lost AND the corrupt commit not actually removed), and `write_work()` still returns the unqualified "Se deshizo el commit (...)" text with zero mention of any anomaly — worse than any round-1 failure mode since it now also destroys someone else's legitimate commit while claiming full success. Same root-cause gap is structurally reachable one step earlier too: `own_commit_sha` itself (`:679`) is captured via a separate subprocess call after `stage_and_commit()` returns, not atomically with the commit — a foreign commit landing in THAT gap poisons the reference the rest of the mechanism trusts. Secondary (folds into the same finding, not separately blocking): the "HEAD moved, refusing to touch history" branch (`:712-724`) never names `own_commit_sha` in its `git_error`, unlike the reset-failure branch which does — makes the corrupt commit, now buried among other history, harder to find by hand than it needs to be. Root fix recommended: replace the check-then-act sequence with an atomic `git update-ref -m <msg> HEAD <parent-sha> <old-value=own_commit_sha>` (fails atomically if HEAD isn't still exactly `own_commit_sha`, no separate check-then-reset gap at all), followed by an index sync only after that succeeds. See [anti-patterns.md](anti-patterns.md) "'Verify after commit, undo with git reset if wrong'" entry, round-2 addendum.

## 2026-08-08 — `notes_commit.py::write_work()` point-8 fix commit-review (`0df6483`)

NOT MERGEABLE — 3 issues, 1 suggestion, 0 nitpicks. Post-commit verify+`git reset --mixed HEAD~1` fix for the TOCTOU window points 6/7 left open. (1) reset's `GitResult` discarded unchecked at `:602` — if it fails (no `HEAD~1` on a first commit, `.git/index.lock` contention from `bin/release.py`/manual git which don't share this module's lock, disk/permission hiccup) the corrupted commit survives as HEAD while `git_error` still unconditionally claims "se deshizo el commit" (`:609-616`), a lying diagnostic stacked on top of the surviving corruption. (2) verification/reset resolve `HEAD`/`HEAD~1` fresh at check-time instead of pinning the SHA captured right after `stage_and_commit()` — the docstring's "nadie más pudo comitear" safety claim (`:517-520`) only holds for callers sharing `memory-notes.lock`, not for any other git-committing process in the repo (confirmed `bin/release.py` never takes this lock); an external commit landing in that window makes `HEAD~1` point at OUR commit, not its parent, so the reset can discard someone else's legitimate commit — reachable even via a transient hash-lookup failure alone, no real race needed. (3) zero new/modified tests for this ~65-line branch (`test_notes.py` last touched `5e682b4`, before this fix); the only test that brushes it is explicitly probabilistic (0/60 unforced historically per its own docstring) and can pass without ever entering the `mismatched` branch. Confirmed correct: the lock genuinely IS held across commit+verify+reset for same-lock callers (no new internal gap there); `known_content=None`-per-path skipping verification is pre-existing disclosed behavior (point 7), not new. Suggestion: `_git_blob_hash_of_bytes()`'s `git hash-object` call has no `--no-filters` and its tempfile lives outside `root` with a name matching no `.gitattributes` pattern — inert today (repo has no `.gitattributes`/`core.autocrlf`, verified) but the "hash git would give these exact bytes" guarantee silently breaks the day one is added. See [anti-patterns.md](anti-patterns.md) "'Verify after commit, undo with git reset if wrong'" entry.

## 2026-08-05 — feat/memoria-v2 pre-merge gate (full `main...feat/memoria-v2` diff, 353 files, scoped pass per task instructions: hooks/skills/agents/release.py/plugin manifests, cross-piece consistency only)

NOT MERGEABLE — 3 T1, 1 T2, 0 T3 worth surfacing. 411/411 `tests/memory` green (one run tripped an unrelated concurrent-commit watchdog false alarm, re-run clean — see false-positives.md). Full non-memory suite: 674 passed, 1 skipped, **8 failed** — all 8 independently corroborate the T1s found by direct diff/parse inspection, not new territory.

T1: (1) `hooks/hooks.json` drops `stop-close-session.py` from `Stop` with zero disclosure, file orphaned on disk, contradicted by `PLAN-CONSTRUCCION.md:127` (says it stays) and the new `TOOLKIT.md` (still narrates it as live 3rd-of-3), confirmed by RED `test_doctor_derived_expectations.py::test_matches_the_hook_files_actually_shipped`. (2) `skills/unmassk-flow/SKILL.md:3` frontmatter is invalid YAML (unquoted colon inside `description`), reproduced via `yaml.safe_load()`, causes 6 of the 8 red tests, risks breaking real skill auto-routing for the toolkit's main build pipeline. (3) `CLAUDE.md`'s own opening banner factually contradicts `git ls-files`/`git status` (claims 5 docs + all memory-v2 code "exist only in the working tree, untracked" — all are tracked and committed) and, as branch-scoped "do not merge yet" narrative with no strip/rewrite step anywhere in the diff, would ship into `main` permanently telling every future session it's looking at an unmerged WIP branch.

T2: `test_doctor_derived_expectations.py`'s shipped-vs-declared check doesn't distinguish the disclosed `customs.py`/`boot_launcher.py` gap (DEUDA #26) from the undisclosed `stop-close-session.py` regression — currently reds on all three, needs an explicit allowlist so future drift on the disclosed pair doesn't mask a real regression. Also 1 more test-suite red not chased individually: `test_user_prompt_skill_router.py`'s `unmassk-close-session__'hand off'` case (stale trigger phrase, same drift class as the flow YAML bug, lower severity — the description just changed wording, it still parses).

Positives confirmed: `bin/release.py` end-to-end wiring verified correct (`notes.write_work()` signature match, `WriteResult.ok`/`.git_error` fields exist, preflight checks fire correctly on live dry-run attempts); all 9 `agents/*.md` GITMEM-resolution fail-open now prints an explicit "command not found" message (T1 from the 2026-08-03 review confirmed fixed, consistently, across all 9 files); `bin/gitmem`'s `SUBCOMMANDS` tuple matches `bin/memory/*.py` on disk exactly; every `gitmem <sub>` example in `skills/unmassk-memory/SKILL.md` matches the real `argparse` flags of its target script; zero residual `unmassk-gitmemory`/`CALIBRATION.md` references anywhere in `skills/`, `agents/`, or `README.md`; `marketplace.json`/`plugin.json` "9 specialized agents" claim matches `agents/*.md` count exactly. See [anti-patterns.md](anti-patterns.md) "feat/memoria-v2 — pre-merge gate round" entry.
