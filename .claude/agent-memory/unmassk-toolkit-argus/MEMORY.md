# Memory Index — unmassk-toolkit (Argus)

- [patterns.md](patterns.md) — no-external-attacker model; git_helpers.py hardened primitives; git `--` convention (evidence gone, principle stands); lib/memory `--issue` round-trip closed; `file_lock()`/`is_safe_path_component()` fixes on checklist_state.py verified closed (2026-08-25); own `git commit` via Bash gets intercepted (not a repo hook — use subprocess/pure-function repro instead); `Note.quote`/`validate_issue_gate` audited clean (2026-08-26); unmassk-trading plugin: lifted gate scripts all exit 0 on every verdict, absence-of-state is a PASS in the breaker, and the fenced block omits the flag the prose demands (2026-08-27)
