---
name: stale-zones
description: Documentation zones known to be outdated or needing review — revisit on next run
type: project
---

## Cleared zones (fixed 2026-08-25, reconciliation pass)

- **Convention confirmed: `docs/plan/` is a LIVE tracker, not frozen history like `docs/deprecated/`.** `docs/deprecated/` carries an explicit "sirvió para construir... la obra terminó, no describe el presente y no se mantiene" disclaimer (`docs/deprecated/LEEME.md`); `docs/plan/` carries no such disclaimer, files are edited on ongoing dates, and the docs themselves already use a dated "[YYYY-MM-DD] Superseded:" correction convention when status changes. Treat `docs/plan/*.md` as due for the same staleness reconciliation as any other live doc when a `gitmem` decision/memo contradicts its stated status.
- Four `docs/plan/*.md` boards said "open/pending" for work that had actually closed: `docs/plan-activar-memoria.md` (issue #82 closed, both remaining points verified executing — header now COMPLETADO), `docs/plan/casillas-por-programa.md` (M-125: shipped 1.39.0, live-tested 2026-08-24 — header now COMPLETADO), `docs/plan/cumplimiento-de-instrucciones.md` (D-052 approved blocks 3/4/5/6/8/9b/9c + program-set checkboxes, D-053 approved block 7; blocks 1-2 deferred-not-cancelled per D-051 — header now "APROBADO PARCIALMENTE"; also corrected block 8.1's premise, which R-010 disproved: `memory: project` is real Claude Code API, must NOT be deleted from the 9 agent cards, contrary to what this plan proposed), `docs/plan/fix-stop-stop-dod-gate-loop-and-issue-field.md` (its one remaining "queda abierto" — a Moriarty round on the declared-red mechanism — is moot since D-046 retired the entire Stop-hook mechanism it would have reviewed).
- Not touched, out of this task's scope: `docs/plan/aprender-de-moria.md`, `docs/plan/fix-dod-gate-classification.md` (not named in the task, not verified).

## Open, flagged not fixed (2026-08-24 close pass, v1.39.0 window)

- `unmassk-close-session/SKILL.md` and `unmassk-council/SKILL.md` have zero mention of the new program-set checklist mechanism (D-052/M-125, shipped 1.39.0) even though `checklists/close-session.json` and `checklists/council.json` exist and gate them via `hooks/checklist-gate.py`. Likely a deliberate omission — the whole point of the design is that the model doesn't need to know in advance, the hook injects the boxes live at Skill-load time — but flagged since close mode's 4-surface table (docs/, module CLAUDE.md, root CLAUDE.md, README) doesn't cover SKILL.md and I didn't touch it. `unmassk-flow/SKILL.md` and `unmassk-audit/SKILL.md` only mention "checklist" in unrelated contexts (trigger phrases, Cerberus's review checklist) — same gap, lower priority since at least a plan doc (`docs/plan/casillas-por-programa.md`) explains the mechanism in full. Re-check on next pass whether the owner wants a one-line mention added. **Re-verified 2026-08-24 (close pass, merge-duplicate-rules window): still zero mention, grep confirms — item still open.**

## Cleared zones (fixed 2026-08-24, not by me)

- Stray file `rules.md` at repo root (flagged in the prior close pass as leftover test/debug output from `gitmem rule`) — removed by the owner directly, commit `05fc1ef` ("quitar rules.md: residuo de prueba... subido por error"), before this close pass started. Confirmed gone (`ls rules.md` → no such file).

## Cleared zones (fixed 2026-08-09, close pass)

- Root `CLAUDE.md` line 15: claimed "lo que quedó abierto... es una sola cosa: la issue #83" — stale, issue #83 closed 2026-08-09 (`gh issue view 83` → CLOSED; X-063/DISCARDED.md: Bex declined to reinstate message-level memory injection because D-041/M-115 already closed the real gap via zone-word search). Corrected in place, not silently dropped — stated the closure and why. This is a **descriptive** claim (root CLAUDE.md's intro prose about present state), code/reality wins, per the file's own rule 12 ("un documento desactualizado no frena trabajo ya hecho").
- `unmassk-toolkit/skills/unmassk-memory/SKILL.md`: zero-result word search now shows the project's zone catalog (shipped v1.35.0, `bin/memory/search.py`) — was a genuine coverage gap, zero prior mention anywhere (README, SKILL.md). Added one paragraph after the commands block. See doc-map.md.
- Checked, not changed: the v1.34.0 atomic-undo-after-commit fix (`notes_commit.py`) has no SKILL.md equivalent to extend (internal integrity guarantee, no usage/output change) — already fully covered by CHANGELOG `[1.34.0]`, which is where it belongs.
- Flagged, not edited (editorial call, not a factual gap): README.md's "Proof, not promises" section is a curated 4-example list of self-caught bugs; the zero-result-search fix would fit the pattern but adding a 5th is the owner's call on emphasis, not a correction of something false.

## Cleared zones (fixed 2026-07-11, issue #63)

- `unmassk-toolkit/skills/unmassk-gitmemory/SKILL.md` "Self-Healing (rebase/reset detection)" + "Force Push Handling" sections (~lines 491-507): described automatic amnesia/history-rewrite detection ("compare known commit hashes", "detect history rewrite... SHAs missing from tree") that never existed as code — pure prose, confirmed by repo-wide grep (zero hits for `amnesia`/`reflog`/`history rewrite` in `lib/`, `hooks/`, `bin/`). This was flagged CRITICAL by the requester because the fiction had persisted undetected through prior Alexandria passes. Rewrote to `### Passive Healing`, describing the real mechanism (live `git log` re-extraction every boot via `extract_memory()`, no stored hash comparison, no reconciliation). **Lesson: a "how we recover from X" section reads as plausible and doesn't get challenged like a feature claim does — actively grep for the implementing code behind recovery/safety-net prose, don't just check it's internally consistent.**
- Same file, "Version marker auto-sync" bullet (~line 108): said `UserPromptSubmit`/per-message; the check moved to `SessionStart`/once-per-session in issue #63. Corrected.

## Cleared zones (fixed 2026-03-24)

- chatroom/CHANGELOG.md [Unreleased] Removed section: falsely said moriarty-system-prompt-v2.md still exists after it was deleted — corrected
- Root CHANGELOG.md: missing [Unreleased] section — added
- Root CHANGELOG.md: duplicate [1.1.0] version tag — old entry renamed [1.1.0-gitmemory]
- chatroom/README.md: test count was 535+ (stale) — corrected to 1200+
- chatroom/CLAUDE.md and chatroom/apps/backend/CLAUDE.md: brainstorm mode undocumented — added
- chatroom/apps/backend/CLAUDE.md: WS message types undocumented — added
- Root CLAUDE.md Protocols menu / build-mode wording gap (fixed 2026-07-04, commit 278b41b): root CLAUDE.md now has `unmassk-flow-stack`, `unmassk-flow`, `unmassk-audit` rows and the "Execute step of `unmassk-flow` (the build pipeline skill)" wording — confirmed via `git diff CLAUDE.md` (empty against HEAD). No action needed.

## Cleared zones (fixed 2026-07-18, issue #72)

- CHANGELOG.md [Unreleased] had zero mention of the anti-attacker test cut (~9.6k lines, 4 files deleted + 6 excised) — genuine coverage gap, not stale wording. Added Removed + Fixed entries. See changelog-state.md.
- Swept README.md, root CLAUDE.md, `unmassk-gitmemory/SKILL.md`, `unmassk-core/SKILL.md`, all plugin READMEs and SKILL.md files for hardcoded test counts ("N tests", "N passed") or anti-attacker-defense-as-coverage claims (control-byte injection, hardlink/symlink-hostile-actor framing) that would now be stale against the cut. **Found none** — no surface outside CHANGELOG.md carried a number or claim tied to this suite. Root CLAUDE.md's threat-model policy section was already accurate (this work implements it, doesn't change what it says); left untouched by design, not oversight.

## Cleared zones (fixed 2026-08-22, close pass — window `ca31435..HEAD`, HEAD `65c57f8` release 1.37.0)

- Root `CLAUDE.md` lines 15 and 38: both said "34 reglas" — `rules.md` grew to 37 during this session (new rule: no analogies/metaphors, added mid-conversation). Fixed both occurrences to 37, added one clause noting the notes/zones counts stay dated to the 2026-08-07 distillation while the rule count is live and grows. **Descriptive claim, code wins** (rule 12 of this same file).
- `unmassk-toolkit/skills/unmassk-flow/SKILL.md` (Project profile section, ~line 71): genuine coverage gap, not stale wording. `bin/stop-dod-declare.py` (declare/clear/status, shields a known test-first in-flight red from the Stop gate) and the tree-fingerprint no-rerun cache both shipped 2026-08-22, already released as 1.37.0, and had **zero mention anywhere** — not in this file, not in `test-first.md`, not in the CHANGELOG. This is exactly the surface that should have caught it (it already documents `stop-dod-gate`'s exit-code classification one sentence earlier). Added 2 sentences extending the existing paragraph — verified the CLI syntax, the `--session "$CLAUDE_CODE_SESSION_ID"` value, and the fingerprint-reuse behavior directly against `hooks/stop-dod-gate.py`'s own docstring and `bin/stop-dod-declare.py`, not from the task brief's paraphrase. Did NOT duplicate into `references/test-first.md` — one home, matching this project's own established pattern of documenting a mechanism once.
- `docs/plan/fix-stop-dod-gate-loop-and-issue-field.md`: status line said "EN CURSO — no publicado" — objectively false, `plugin.json`/`marketplace.json` both show 1.37.0 released (commit `65c57f8`, same session). This plan doc is also `unmassk-flow`'s own "one feature in flight" gate input (`grep -L 'Status: COMPLETED' docs/plan/*.md`) — being the only plan without that exact string meant Flow would treat already-shipped work as still blocking new work. Changed to `**Status: COMPLETED**` (the literal string the gate greps for) with the publish evidence inline, and struck through the "Lo que falta para cerrar" checklist against real evidence for items 1/4/5. **Left honestly unresolved, not silently marked done:** item 2 ("prueba en directo, exigida por el propietario" — full manual test-first cycle against a real project) and a dedicated Moriarty round on this specific mechanism ("Caso 17") — searched Moriarty's `round-history.md` and Dante's own contract notes, found zero record of either. Flagged in the doc itself as a question for the user, not resolved unilaterally.

## Cleared zones (fixed 2026-08-23, close pass — window `7f4e765..d46444f`, HEAD `d46444f`, release 1.38.0 same window)

- **`CHANGELOG.md` `[1.38.0]` had a real commit misattributed to it** (not stale wording — a factual placement error the orchestrator's own entry made): the `gitmem work`/`git rm` staged-deletion fix (`lib/memory/notes_commit.py`) was listed under `[1.38.0] → Changed`, but `plugin.json` at the fixing commit (`5f6b513`) still reads `1.37.0` — the fix shipped inside the `1.37.1` release window (bumped by `cfbf668`), a full release before `1.38.0`. Verified by reading `plugin.json` at every commit between the two release tags, not by trusting either commit message. Moved the bullet to `[1.37.1]` and reclassified it `Fixed` (a bug — `git add --all` on a path already staged for deletion via `git rm` exits 128, blocking the whole commit — not a `Changed` behavior). `[1.37.1]` previously had zero `### Fixed` section; added one. Wording ("it used to fail on the `git add` step") was already accurate against `notes_commit.py`'s own docstring (`stage_and_commit()`, the 2026-08-23 dated paragraph) — kept verbatim, only the placement and header changed.
- Test-count claims in `[1.38.0]` re-verified directly (`pytest --collect-only`, not from the entry's own numbers): "51 tests" for `test_skill_router_spanish.py` = 51 collected, exact. "11 tests" for `test_rule_quote.py` = 11 collected, exact. No correction needed.
- Root `CLAUDE.md` lines 15 and 38: both said "37 reglas" — `rules.md` grew to 42 during this session (`grep -c '^\[remember\]' .claude/project-memory/rules.md`). Fixed both. Also added the missing "El CHANGELOG lo escribe Alexandria" bullet to "Reglas vivas" — genuine coverage gap, see doc-map.md.
- `docs/plan/aprender-de-moria.md` — checked, NOT touched: items 1 ("Dante y Ultron pierden la herramienta de lanzar agentes"), 3 ("Enrutador en castellano"), 4 ("Fichas de Argus y Bilbo") are all verifiably shipped in code (agent-card `tools:` lines missing `Task`; `lib/skill_router.py::REMINDER_TRIGGERS`/`match_reminders()`; `EXECUTED`/`READ`/`UNVERIFIED` tags in `agents/argus.md`+`agents/bilbo.md`) but their checkboxes still read `[ ]`. This is the owner's own working board (per the file's own header: "se va de uno en uno, con el sí de Bex en cada punto") — same treatment as every prior `docs/plan/*.md`/`roadmapv2.md` precedent in this file: Alexandria flags, doesn't tick boxes on someone else's board. Flagged to the user in the close report instead.
- `docs/plan/fix-dod-gate-classification.md` and `docs/plan/fix-stop-dod-gate-loop-and-issue-field.md`: both already carry `[2026-08-23] Superseded: the gate was retired by the owner; see D-046.` at the top — correctly done by the building agents this session (Alexandria only verified, did not author).
- `M-117` (TodoWrite re-enabled via `CLAUDE_CODE_ENABLE_TODO_TOOLS=1` in `~/.claude/settings.json`) — confirmed outside the repo, no CHANGELOG entry needed (project convention: only repo code changes go in CHANGELOG). Memo exists (`MEMOS.md` line 103). No further doc surface applies — it's a local machine setting, not a project convention.

## Cleared zones (fixed 2026-08-23, D-053 doc restructure)

- `unmassk-toolkit/skills/unmassk-scaffolding/references/frameworks.md` (800 lines) split into `references/frameworks/{react,nextjs,vue,nuxt,sveltekit,fastapi,django,express,nestjs,cross-cutting}.md`, `frameworks.md` rewritten as a 28-line index. `SKILL.md` already described this exact target layout (lines 50/266/272, "INDEX ONLY... load ONLY the per-framework file") before the split happened — confirms this was pre-planned, not improvised. Full line accounting done (800 = 762 moved + 38 deleted separators/category headers/title block, verified exactly). Canonical Vitest config (in `cross-cutting.md`, the only vitest.config.ts in the source file) got `coverage.thresholds` added at 80% per the task's fix (a). Fixes (b) (next.config.ts coherence) and (c) (npm-as-default) were checked and found already satisfied / not applicable — zero `next.config.js` or `pnpm`/`npm`/`yarn` mentions existed anywhere in the source file, nothing to change.
- **Flagged, not touched** (file was out of scope — SOLO the frameworks.md file was mine): `unmassk-scaffolding/references/best-practices.md:235` still says "Full detail... in `frameworks.md`" — not false (the index still leads there), but now one hop indirect since frameworks.md no longer holds the detail itself. Also noted `SKILL.md`, `wizard-options.md`, and a new `wizard/` dir show as already modified/untracked in git status, presumably a parallel pass on the same D-053 restructure — not mine, not verified by me.

## Active stale zones

### Fixed 2026-08-03 (later same day) — `DEUDA.md` PARTE 2, points 13/26/27 + header counts
Task brief claimed point 13 (capas 2/3 review) was closed via "Moriarty pasó por tercera vez [clean]". **Independently verified this was wrong**: Moriarty's own `MEMORY.md` "Last attack" entry (his actual third capa-2/3 pass, dated 2026-08-03, self-labeled "third and final") gave verdict **FALLA**, not a clean pass — it found 3 things, not the "only point 27 was still open" the brief claimed. Two of those three are genuinely fixed (verified independently, not taken on the brief's word): the ordinary two-writer `write_work()` race (closes point 27, confirmed via Dante's `known_content` ablation test — reproduced RED 3x at 9/20, 6/20, 5/20 before the fix, 0/60 after) and `rezones.py --rebuild` not committing its repair (confirmed live by me: seeded a real repo, corrupted an index, ran `rezones.py`, then `git checkout --` on the repaired file — the repair survived). The third — a rogue actor writing to disk without ever calling `write_work()` — is admitted-still-open in `notes_commit.py`'s own docstring ("no eliminado", only narrowed). Point 13 stays open in DEUDA.md; the closure claim in the task brief was corrected, not accepted. Also closed point 27 (`[x]`), updated point 26 (removed stale `hooks/inject.py` reference — deleted along with `lib/memory/dispatch.py` per decision B20), corrected the stale "275 verdes" test count to 261 (re-ran `pytest unmassk-toolkit/tests/memory -q` myself), and added a dated final tally (14 closed / 13 open of 27, up from 13/14) superseding the doc's own prior conflicting tallies. **Lesson for next DEUDA.md sync: a task brief's "X passed cleanly" claim about a Moriarty/Cerberus review must be checked against that agent's own MEMORY.md `Last attack`/review-history entry directly — this project has already hit "marked closed, wasn't" as its worst-case failure once (per the project's own root CLAUDE.md), and the pattern repeats: selectively citing the HELD parts of a verdict while omitting the FALLA parts it came bundled with.**

### Fixed 2026-08-03 — `docs/memoria-v2/PIEZAS.md` against the owner's 2026-08-03 decisions (DEUDA.md Parte 1 + a second, undocumented decision found mid-task)
Corrected: script renames (`close`→`remove`, `context`→`next`, `reindex`→`rezones`), `bench` deleted, `boot` removed from the gitmem-subcommand table (still exists as a file, just not dispatched), `wip.py` added (new CAPA-5 row + a new §10.1 point describing its shared branch-guard). Fixed the `context_points: tuple[str,...]` → `context: str` (prose) field to match real code, the two-int coherence check → `archived_notes` breakdown ("587 live + 25 archived / 612 notes"), and English-labeled structural blocks (`BLOCKERS`/`RESTRICTIONS`/`CHECKS`/`COUNTS`/`[PROJECT MEMORY]`) that were still Spanish in byte-compared example blocks. §14's bench-adversarial-catalog entry point was rewritten as an explicit `[pregunta]` (not decided unilaterally, per the doc's own §0.2 rule) since its only stated entry point (`gitmem bench`) is gone.
**Second decision found mid-task, not in the original brief, fixed for internal consistency:** the `[NEXT]`/session-close commit format changed a second time on 2026-08-03 (confirmed in real code — `lib/memory/emojis.py`, `lib/memory/model.py`, `bin/memory/next.py` — and in `TEXTOS.md §5`, already updated by a parallel pass): marker `⏩` → literal bracket `[NEXT]` + `🧭` (reusing the decision glyph), same decision that made the body prose instead of bullet points. Propagated this across every place PIEZAS.md described the old `⏩` form (§5.2 CHANNEL_EMOJI, §5.3 ContextNote, §6.4, §9.5, §9.6, §9.7, §11/§11.1) — leaving it un-updated in some of those spots while fixing others would have made the document contradict itself, which the task's own closing instruction required checking for.
**Flagged, not fixed (out of the assigned scope, noted inline in §10.1 point 4 instead):** `lib/memory/repo_guard.py` is a real, in-production CAPA-2 module (shared by `work.py`/`wip.py` for main-branch protection) with no 7-section ficha of its own in §7 — a genuine gap for a future pass.
**Lesson:** when a task gives a scoped list of line-numbered fixes, always check whether the surrounding paragraph refers to a *different*, more recent decision than the one named in the brief — `git log`/code beats the task prompt's own framing when they disagree, and DEUDA.md's own "decisiones tomadas hoy" section is not guaranteed to be complete/final within the same day.

### Checked 2026-08-03 — `docs/memoria-v2/TESTIGO.md` against the owner's 18 same-day decisions (DEUDA.md Parte 1) — no contradiction found, left untouched
Task was to correct TESTIGO.md against every decision the owner made 2026-08-03 (9 commands renamed/removed/added, English structural labels, valla→muro + emoji variant, session-close prose format, report-by-identifier mold, coherence-check breakdown, git-erasure-command confirmation rule, nested-repo non-fix, rules default to `user`, boot greeting shape). Grepped the whole file for every command name (old and new), every structural label (RECUENTOS/AVISOS/RESTRICCIONES/BLOQUEANTES/MEMORIA), `valla`/`⚠`, note-type codes (D-/M-/I-/R-/Q-), `corona`/`compact`/`rescat`/`crown` — zero hits except incidental. TESTIGO.md is pure v1-measurement (frozen at commit `ecdfaa5`, dated 2026-08-02): it lists dead v1 functions (`recall_relevant`, `_crown_replace`, `precompact-snapshot.py`) as evidence for capabilities the owner cut today (fuzzy search, crowns, pre-compact rescue) — this *reinforces* those decisions with numbers, it doesn't assert anything that contradicts them. It never names a v2 command, an English/Spanish label, or the session-close format, so none of today's renames or reformattings apply to it. Genuine "nothing to fix" result, not a missed-search.

### Fixed 2026-08-02 (second pass, same day — explicit fix request, not just flagging)
- `unmassk-toolkit/skills/unmassk-flow/references/linear.md:24` and `references/test-first.md:39,46` — repointed the false "standards.md §7 (backend) and §27 (frontend)" citation to the sections that actually exist (§1 tier system, §9 Real-verification checklist, §34 round-trip). Also dropped `test-first.md`'s external-attacker test items (manipulated token, privilege escalation) — out of this toolkit's own threat model — replaced with the internal-failure items §4/§9 actually cover.
- `unmassk-toolkit/skills/unmassk-audit/prompts/*.md` (all 9 templates) — replaced hardcoded `backend/src/[MODULE]/`, `npx vitest`, `npx prettier`, `Zod` with `[MODULE_PATH]`/`[TEST_CMD]`/`[FORMAT_CMD]`/`[LINT_CMD]` placeholders resolved from the project profile (same mechanism as `unmassk-flow`'s "Project profile" section — explanatory note added once in `SKILL.md`'s "Prompt Templates" section, not repeated per file). Deliberately did NOT touch `prompts/cerberus.md`'s separate, unrelated staleness in this same file (`docs/ENTERPRISE-STANDARDS.md` — a path that doesn't exist, should point to the `unmassk-standards` skill instead; old §4.5/§7/§1 numbering; weighted-dimension names that don't match `standards.md`'s real table; an auth/routing-specific "critical rule") — out of the 4-item brief for this pass, flagged below instead.
- `unmassk-toolkit/skills/unmassk-close-session/SKILL.md` frontmatter `description` — rewrote to match the real 5-step housekeeping body (was promising decision-consolidation + resume point, which the body doesn't do since the old memory system's flush step was retired on `feat/memoria-v2`). Added an explicit in-file note that this returns at plan step 7.10.
- Root `README.md` — removed the "Calibration" row (Core components table) and the `git-memory-recall.py` row (Key scripts table): the calibration mechanism and that script are both gone from disk (confirmed `CALIBRATION.md`/`unmassk-gitmemory/` skill dir don't exist anywhere in the repo; `git-memory-recall.py` absent from `unmassk-toolkit/bin/`, only an orphaned `.pyc` remains). Updated the `unmassk-close-session` one-line description in the Protocols row to match the SKILL.md fix. Left the "Memory" row's other two claims (boot pull-proposal, Bilbo dead-end persistence) untouched — verified both are still real (`lib/boot_git_checks.py:279`, `agents/bilbo.md`'s dead-end mechanism) — do not touch on a future pass without re-checking.
- `.claude-plugin/marketplace.json` — `unmassk-toolkit` plugin description dropped the same "memory calibration" phrase.
- CHANGELOG.md `[Unreleased]` got one Fixed entry per item above (4 bullets total).

### Fixed 2026-08-02 (third pass, same day — self-detected during the second pass, then explicit fix request)
- `unmassk-toolkit/skills/unmassk-audit/prompts/cerberus.md` (both templates) — `docs/ENTERPRISE-STANDARDS.md` (never existed) repointed to `unmassk-standards` (`references/standards.md`); old numbering (§4.5/§4.3/§7/§1/§3/§6/§5/§11) repointed to the real sections (§2/§3/§4/§5/§6/§7/§9/§11/Pilar-1-§34); the stale `Security x3 / Error handling x3 / Structure x2 / Testing x2 / Maintainability x1` table replaced with the real one (Integrity ×3, Silent-failure/Error handling ×3, Structure ×2, Real verification ×2, Maintainability ×1, verified by reading `standards.md` §9 directly, not from memory). The "verify middleware before reporting auth/routing" critical rule (Node/Express-specific, and moot — standards has no Security dimension) replaced with a stack-agnostic "verify upstream context before reporting" rule, in both templates.
- `unmassk-toolkit/skills/unmassk-audit/prompts/argus.md` — "Focus areas" list (OWASP patterns, auth/authz design, SQL injection surface) was the same external-attacker framing `CLAUDE.md` explicitly excludes from this toolkit's threat model. Replaced with the internal-integrity surfaces Argus actually owns per `unmassk-standards/SKILL.md`'s own agent-usage table (memory/persistence integrity §3, silent failure §4, concurrency §6, platform robustness §5, round-trip Pilar 1/§34). File and template renamed "security audit" → "integrity audit" throughout for consistency with the new content.
- `unmassk-toolkit/skills/unmassk-audit/SKILL.md` — same 3 issues found in its own text once the templates were checked against it: step 4's Argus description still said "OWASP patterns, auth design" (renamed to match `argus.md`'s new integrity framing); the Findings Report Format table's scoring-dimension parenthetical still listed the old `Security x3/Error handling x3/Testing x2` names (corrected to the real 5); the "Additional Resources → Standards" line claimed `unmassk-standards` covers "OWASP" (false — verified zero OWASP mentions in `standards.md`, corrected to state explicitly there's no OWASP/external-attacker material). Left untouched: the 97%-coverage-as-explicit-audit-exception language (steps 3/7/Quick Reference) — that's a separate, already-resolved item (marked as a conscious documented exception, not a silent contradiction) and was out of this pass's brief.
- Swept the other 7 templates (`bilbo.md`, `dante.md`, `house.md`, `moriarty.md`, `ultron.md`, `yoda.md`, `alexandria.md`) for the same two classes of drift (broken doc/section reference, external-attacker framing) — grepped for `ENTERPRISE-STANDARDS`, `§`, `OWASP`, `injection`, `privilege`/`escalad`, `hostile` across the whole `prompts/` dir. Found none in those 7 — clean.
- CHANGELOG.md `[Unreleased] → Fixed` got one new bullet covering all of the above (appended to the same-day entry, not a new header).
- `unmassk-toolkit/bin/hooks_doc_sync.py` targets `skills/unmassk-gitmemory/SKILL.md`, which no longer exists in this repo (moved/renamed) — confirmed `ls` fails on that path. Script has zero automatic callers (not in any hook, skill, script, or CI workflow); its comparison logic (not the rewrite logic) is separately imported by `git-memory-doctor.py`, which does run every session.
- Hooks `stop-dod-check.py` and `stop-close-session.py` compute their reminders correctly but emit via a `Stop`-event stderr channel measured at 0/2506 delivered to the model (comment in `lib/incidents.py`); `user-prompt-memory-check.py`'s channel is 1445/1445. Known by the team, not yet moved.
- `unmassk-toolkit/skills/unmassk-close-session/SKILL.md` frontmatter `description:` promises "nothing decided is lost, next session can pick up where this one left off" but the 5 numbered body steps are pure housekeeping (version/changelog/cleanup/branches/doc-check) — no decision-consolidation or resume-point step. The body's own opening `> Note` already explains this split (a `Stop`/`PreCompact` hook owns the guarantee now, this skill is just the housekeeping content) — so the body is internally honest, only the short frontmatter description used for skill-routing still oversells alone. Minor, cosmetic.
- `unmassk-toolkit/tests/test_pre_validate_commit_trailers_git_log.py` (lines ~10, ~132) cites path `agents/gitto.md:239` in an explanatory comment — that file moved to `deprecated/gitto.md` when Gitto was retired. This is the *only* live Gitto trace anywhere outside `deprecated/` (confirmed via repo-wide `grep -rni gitto` excluding `deprecated/`) — `unmassk-flow/SKILL.md` itself has zero Gitto mentions today (a prior draft claimed otherwise; that claim was checked and is false, see TOOLKIT.md's doc-map entry).

### Root CHANGELOG.md [Unreleased] — two separate `### Fixed` headers (noticed 2026-07-18, not fixed)
Current `[Unreleased]` has `### Fixed` twice (once right after `### Removed`, once after `### CI`), from separate passes that each appended their own header instead of merging into the existing one. Cosmetic against Keep a Changelog ("group by type", implying one header per type per version) — not a factual error, so left as-is rather than restructuring unprompted. Worth collapsing into a single `### Fixed` block next time `[Unreleased]` is touched for an unrelated reason, or whenever `bin/release.py` next promotes this section to a version tag.

## Cleared zones (fixed 2026-07-16)

- Root CHANGELOG.md [Unreleased] Added — the `unmassk-3d` and `unmassk-frontend` entries said Blender/AgentBrowser were "wired in `.mcp.json`" / "wired via a plugin `.mcp.json`" without mentioning on-demand registration (both `.mcp.json` files ship empty; servers register via `claude mcp add` the first time the skill needs them). Rewrote both to name the on-demand mechanism explicitly, consistent with the new `[Unreleased] → Changed` entry covering the other 6 plugins.

### unmassk-core/SKILL.md:40 — unmassk-design skill count stale (flagged 2026-07-14, not fixed by Alexandria)
Says `unmassk-design | 1 skill | Design systems, color, typography, motion, accessibility, agentic UX` — now stale, the plugin revamped to 7 skills (core + design-motion/design-3d/design-scroll/design-animation-formats/design-taste/design-flutter) on 2026-07-14. Root `README.md`'s matching row was fixed this same pass. `unmassk-core/SKILL.md` is explicitly orchestrator-owned per the 2026-07-12 precedent below (core/gitmemory/audit SKILL.md are the orchestrator's own docs) — flagged only, not edited. Re-check on next pass whether the orchestrator updated it.

### unmassk-core/SKILL.md and unmassk-audit/SKILL.md — BM25 skill-search claims, orchestrator-owned (flagged 2026-07-12, not fixed by Alexandria)
`unmassk-core/SKILL.md:44` says "the agent runs BM25 skill search and loads the matching skill automatically" — contradicts `:77` which correctly says agents no longer search themselves. Both are now stale against the current reality (BM25 gate retired entirely 2026-07-12, decision `5d660b4`, tag `bm25-skill-gate-1.19.9`): the orchestrator loads all skill frontmatter and picks/injects by criterion — there is no gate hook doing BM25 anymore either. `unmassk-audit/SKILL.md:273` says unmassk-standards loads "via BM25 skill search" when it actually loads via frontmatter (`skills:` in agent frontmatter). These 3 files are explicitly orchestrator-owned per task instructions (not Alexandria's — core/gitmemory/audit SKILL.md + agents/*.md are the orchestrator's own docs), so Alexandria only flags, doesn't edit. Re-check on next pass whether the orchestrator has fixed them.

### chatroom/CHANGELOG.md [Unreleased] — no version stamp
Recent commits (Tauri 2 shell, stdin delivery, repo cwd selector, security hardening, tab title, queue messages) are NOT in the chatroom changelog at all. The [Unreleased] section covers up to the LOC refactor but not the more recent frontend/backend changes. If chatroom cuts a release, a significant backfill is needed.

### chatroom/docs/ folder — not deeply audited
Docs may have drifted given volume of changes. Priority files to re-verify: websocket-protocol.md (brainstorm mode, clear_queue, stop_all not documented there), agent-invocation-pipeline.md (brainstorm mode filter not shown in the spawn example), module-reference.md.

## Cleared zones (fixed 2026-07-05)

- project-context.md memory file version was stale (said 1.1.1) — corrected to 1.15.0.

## Cleared zones (fixed 2026-07-07)

- SKILL.md `unmassk-gitmemory` had zero mention of the boot-time `git fetch`/freshness behavior at all (not stale wording — a genuine coverage gap, the whole issue #49 feature was undocumented for Claude). Added Boot Protocol paragraph + Wrapper Scripts bullet. Root README.md Memory row didn't mention multi-machine sync either — added one clause.

