---
name: changelog-state
description: Tracks the last changelog update date and what was included, so future runs only process new commits
type: project
---

## Bookmark

**Last commit that actually touched `CHANGELOG.md` on disk: `30c2fbd` (2026-08-27, release unmassk-trading v1.0.0).** A newer working-tree edit exists (2026-08-27 later pass, not yet committed) covering `30c2fbd..HEAD` — two code commits only (`58f6889` CLAUDE_PLUGIN_ROOT paths, `30a4e55` skill review pass), plus `c2728cd` (maker-plugins CI Python 3.10→3.12) which actually landed BEFORE the 1.0.0 release commit and was never documented there. Wrote 5 `### Fixed` / 4 `### Added` / 2 `### Changed` bullets, all in Spanish matching the surrounding entries, all pointing at `[#85](../../issues/85)` plus R-018 (paths) and M-133 (CI). No `Known` section: nothing new ships broken, and the owner explicitly said not to repeat 1.0.0's loss-brake `Known`.

**Version-header precedent CORRECTED 2026-08-27** (the old note in this file said the opposite and was wrong): `unmassk-trading` DID get its own `## [1.0.0] - 2026-08-27` header, sitting above `## [1.42.0]` (the toolkit track). So per-plugin version headers now coexist with the toolkit track in the same file, newest-first by date regardless of which track the number belongs to. `bin/release_helpers.py:_preflight_resolve_paths` still resolves `CHANGELOG_REL` from the repo root for every plugin, which is exactly why the ROOT `[Unreleased]` is where a trading release's content goes.

**Consequence worth remembering:** anything non-trading written into `[Unreleased]` (e.g. the maker-plugins CI fix) will be promoted under a `[1.0.1]` *trading* header. Flag that to the owner rather than silently filing it; it is a real mismatch of the shared-file/per-plugin-header scheme, not a mistake in the entry.

**Excluded this pass:** `7e0aa6c` (R-018 note) and `bee1fe6` (M-133 note) — memory commits, per the standing rule.

## Superseded bookmark (kept for index continuity)

**Last commit that actually touched `CHANGELOG.md` on disk: `85545f7` (2026-08-26, 10:05).** A newer working-tree edit exists (this pass, 2026-08-26 later) not yet committed — covers `a100b4e`+`9d444e3`, the "aduana de issues" feature (D-065/D-066: `Q`/`I` note save rejects without `--issue`/`--work`, `--work no` bypass, `--issue none --quote` with the owner's literal phrase, new `Note.quote` field, verified against `bin/memory/note.py`:29-42/102-138 and `lib/memory/model.py`:73, 18-test contract in `tests/memory/test_note_issue_gate.py` — 8 `def test_` × parametrize = 18 real cases, matches KNOWN). Added one `### Added` bullet for it, and a second `### Changed` bullet for D-068 itself (the entries-compression rule, previously applied to existing entries 2026-08-26 but never given its own changelog line — closed this pass, explicitly requested).

Previous content (committed in `85545f7`, superseded content below kept for index continuity): `### Added` (`BootSummary.issues` — `lib/memory/model.py`/`boot.py`, feature bundled inside commit `65f47a0` whose message only names an unrelated CI fix; contract tests `test_boot_issues_field.py`); `### Fixed` (CI Ubuntu `path_without_real_gh` 37-red fix `65f47a0`+regression test `4249f6b`, Windows `.exe`/PATHEXT `1b8d027`, Windows autocrlf corrupt-object fixture exemption `9346c8b`, House card `git grep -I` `d9cec70`, v1-sweep dead-reference cleanup `5e1f1f1`+`246d40e`, test suite realignment T26/`deep_search` retirement `75fdbbe`). The pre-existing `### Changed` bullet (8-section opening menu) was verified still faithful against `unmassk-memory/SKILL.md` (now correctly says "eight", the "five" drift flagged 2026-08-25 is resolved) and left untouched.

**Excluded this pass (verified memory-only, no product code touched):** `[R-014]`..`[R-017]` restriction commits (`ba23a71`/`ac53f3a`/`61ce67b`/`2ecfeb0`), 9-agent + Dante-specific memory compaction (`7525183`/`8ed14f8`/`ef2fd29`), Yoda verdict notes `[M-126]`/`[M-127]` (`8d12fe0`/`106d61a`), and 4 more `.claude/agent-memory/*`-only commits (`38334bb`, `3019e23`, `e43dc2c`, `9a8591f`) — all confirmed via `git show --stat` to touch only memory files, matching the established "memo/decision/context commits never go in CHANGELOG" rule; agent-memory reorganization judged equivalent (repo-local housekeeping, not a shipped behavior change) since no prior CHANGELOG entry exists for any past compaction pass either.

**On next run:** once this working-tree edit is committed, `git log <that commit>..HEAD --oneline -- CHANGELOG.md` first to see if anyone else already updated it; then the same range for the full commit list.

Added a second `### Changed` bullet same pass (2026-08-26): Alexandria's own agent card (`unmassk-toolkit/agents/alexandria.md`) gained a "Markdown Style — full width, always" section (owner-dictated from an AlSara session) plus a CHANGELOG-Rules line pointing at it; reviewed word-for-word on request (self-consistency check: the new prose is itself unwrapped, e.g. one 283-char line; no contradiction found against Diátaxis 150-line-per-file or any other existing rule) and reported clear/executable/agnostic with no edit needed — card is orchestrator-owned, read-only for Alexandria.

**D-067 pass (2026-08-26, same day, later):** card's `CHANGELOG Maintenance` section changed again — Keep a Changelog base + 3 deviations (Fixed first, issue-links, `Known` section closing a release). Reviewed word-by-word (card still read-only): found one real inconsistency — the header prose says an issue-linked entry reads `(#N)` (bare parenthetical, no hyperlink) while the worked example and the rules-list entry two paragraphs down both use a full markdown link `([#N](<repo issues url>/N))`; following the header literally would not produce a clickable link. Flagged to the orchestrator, not self-fixed (card is read-only for this agent). Also flagged, not resolved: `gitmem search --id D-067`'s own `Why` line hedges — "order is convention not rule, links are encouraged, only Known is a deliberate deviation from its six types" — while both the note's `Description` and the applied card text treat all three as unconditional rules (no "convention"/"encouraged" language anywhere in the card); a real tension inside the decision record itself, reported rather than silently resolved since a decision note is prescriptive. Applied the new order to `CHANGELOG.md`'s `[Unreleased]` (Fixed → Added → Changed, entry text untouched, no `Known` section invented — nothing ships broken/known right now) and added one `### Changed` line documenting the format change itself.

**D-068 pass (2026-08-26, same day, later):** card's `CHANGELOG Maintenance` section gained a third format piece — each entry capped at 1-2 sentences with the why-it-matters in the line itself, closed with a pointer to its git-memory note/issue instead of repeating the story (verified via `gitmem search --id D-068`: text matches the card almost verbatim). Reviewed word-by-word (card still read-only): found the pointer rule names only `D-xx`/`I-xx` as valid note-id types, but this same pass needed to cite `M-127` (a memo, verified via `gitmem search M-127`) as the correct pointer for three CI/Windows fixes — the enumerated types don't cover the type this task itself uses. Also flagged: the line doesn't say the pointer is conditional on a note existing, so read literally it could be misapplied as mandatory on every entry, which contradicts entries that have no note behind them (e.g. the House `-I` fix, the dead-reference sweep). Proposed exact replacement text to the orchestrator (broaden to "`D-xx`, `I-xx`, `M-xx`…" and add "when a note stands behind the entry"), not self-fixed. Applied the rule to `CHANGELOG.md [Unreleased]`: 9 entries compressed to 1-2 sentences each, `D-060/D-064` (already cited) kept as Added's pointer, `M-127` added to the 3 CI/Windows Fixed entries it verifiably corresponds to (per its own note text), no pointer invented for entries with no note behind them. Did not add a new entry documenting D-068 itself — out of this task's literal scope (compress existing entries only).

## Durable lessons

- **Never write a manual `## [N.N.N] - date` header in `CHANGELOG.md`.** `bin/release.py` promotes `## [Unreleased]` to the version header itself on release — a manually-added header means `release.py` sees an empty `[Unreleased]` and aborts. Content always goes straight under `## [Unreleased]`, always. (Learned 2026-08-25, first pass of that day — added a manual `[1.40.0]` header, had to remove it same pass.)
- **Verify a CHANGELOG entry's version attribution against `plugin.json` at the actual fixing commit, not the commit message's own claim or which release window it "felt like."** A real misattribution happened 2026-08-23: a bugfix was dated `[1.38.0]` in the entry but `plugin.json` at the fixing commit still read `1.37.0` — it had actually shipped inside `1.37.1`, one release earlier. Fixed by reading `plugin.json` at every commit between the two release tags.
- **`memo`/`decision`/`context`/`wip`/`remember` commit types never go in the CHANGELOG** — they're the memory system's own audit trail, not shipped product changes (see project-context.md). Also excluded by the same logic: edits to `roadmapv2.md`/`ROADMAP.md` alone (confirmed by scanning its full history — 20+ commits, none ever became individual CHANGELOG entries; contrast with the 1.12.0 commit/push-cadence entry, which changed real shipped SKILL.md behavior and correctly was included).
- **Verify every claim in a task-supplied "KNOWN" bullet list against the actual code/skill file before writing the entry — don't transcribe it as given.** This has caught real drift more than once: a task's KNOWN block claiming a fact was anchored in "the managed block of CLAUDE.md" turned out false on grep (2026-08-25, `unmassk-groundhog` pass); a task's paraphrase of a diff described a "CSS combo" fix that the actual diff didn't contain (2026-08-24 pass, wrote what the diff really showed instead).
- **Markdown Style rule, effective 2026-08-26 (owner-dictated, added to `unmassk-toolkit/agents/alexandria.md`): one line per paragraph/bullet, never hard-wrapped at a column width, in every document Alexandria writes or edits** — CHANGELOG, README, docs/, CLAUDE.md alike. Already the de-facto practice in this file's own CHANGELOG entries (verified before this rule existed); now codified and mandatory going forward, including for `.claude/agent-memory/*` topic files this agent writes.
- **A doc-only/CHANGELOG-only pass still owes a check of the 3-surface rule** (README/docs, CHANGELOG, SKILL.md/CLAUDE.md) even when scope is restricted to one file — flag what's out of scope rather than silently skip it. Recurring pattern: several 2026-08-25 passes found the skill prose itself had drifted (e.g. "always these five labeled sections" left un-updated after the table grew to eight rows) while fixing only the CHANGELOG bullet — correctly flagged, not fixed, out of that pass's file scope.

## Historical pass index (compact — full narrative recoverable via `git log` for each range)

| Date(s) | What shipped | Notes |
|---|---|---|
| 2026-08-26 | `BootSummary.issues`, CI Ubuntu/Windows fixes (path_without_real_gh, PATHEXT, autocrlf), House card `-I` fix, v1-sweep dead-reference cleanup, test suite realignment (T26, deep_search retired) | Pre-release full pass since v1.41.0; see Bookmark above |
| 2026-08-25 (4 same-day passes) | 8-section opening-menu table (D-060/D-062/D-063/D-064), `unmassk-groundhog` skill, v1.40.0 memory-legibility batch | See Bookmark above for final state |
| 2026-08-24 | Program-set checklists (D-052/M-125), shared delegation template + blind-review template, "excuse" tables on all 9 agent cards, `unmassk-scaffolding` reference split, `lib/memory/rules.py`/`zones.py` split | Prep for 1.39.0 |
| 2026-08-23 | Modo automático, Spanish-phrase skill router, `gitmem rule --quote`, Argus/Bilbo verification tags, Dante/Ultron lose `Task` tool | 1.38.0; misattribution fix noted above landed same pass |
| 2026-08-20 | `stop-dod-gate` exit-code classification (D-042) | Later fully retired by D-046 |
| 2026-08-06 | `hooks/customs.py` rescue-command robustness, `stop-dod-gate.py` corrupt-config handling, CI hardening | Pre-1.29.2 |
| 2026-08-02 – 2026-08-05 | memoria-v2 build/merge — false-promise fixes in `unmassk-audit`/`unmassk-flow` prompts, `feat/memoria-v2` → `main` merge gate (the `gitmem` 9-subcommand system itself) | Target docs since moved to `docs/deprecated/`, see doc-map.md |
| 2026-07-25/26 | Trailer-content validation moved to the wrapper, dead validation-hook layer retired | 1.24.0 prep |
| 2026-07-18 | Issue #72 (anti-attacker test cut, ~9.6k lines) + issue #61 (9 read sites wrapped in retry) | |
| 2026-07-16 | MCP on-demand pattern rolled to 6 plugins | See doc-map.md "Other plugins" |
| 2026-07-13/14 | `unmassk-pentesting` v1.0.0 (30 skills), `unmassk-design` 1→7 skills | See doc-map.md "Other plugins" |
| 2026-07-12 | BM25 skill-search gate retired entirely | |
| 2026-07-04 – 2026-07-11 | Boot simplification (issue #63), issue #60 (freshness stamp), issue #55 (date parsing), issue #49 (multi-machine freshness) | |
| 2026-03-14 – 2026-03-24 | chatroom docs/JSDoc, root CHANGELOG structure fixes (duplicate `[1.1.0]` tag renamed) | |

**Why:** Alexandria needs to know where to resume on next launch — only commits after the bookmark need processing. The historical index exists only so a date can be located; it is not a substitute for reading `CHANGELOG.md` itself, which already has the real entries.
