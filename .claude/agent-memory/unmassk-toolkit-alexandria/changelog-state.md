---
name: changelog-state
description: Tracks the last changelog update date and what was included, so future runs only process new commits
type: project
---

CHANGELOG pass 2026-08-25, third same-day pass (explicit request, scope restricted to `CHANGELOG.md` only, task LIMITS: no other file touched, no commit — final state-of-the-day rewrite of a single `[Unreleased] → Changed` bullet about `unmassk-memory`'s opening-session menu, replacing rather than adding to the bullet the second pass of the same day had already written): read `unmassk-toolkit/skills/unmassk-memory/SKILL.md`'s real "Opening and closing" section (lines 244-260) before writing, not the task's KNOWN block as-given. Found one real gap versus the existing CHANGELOG bullet: the sixth ⚙️ Health row (appears ONLY when something is off — branch mismatch/unpushed commits, a failed boot coherence check listed first, issues carrying a live note; does not exist at all when everything is clean) was completely undocumented. Rewrote the bullet in place (single entry, no duplicate) to add the Health row plus two small precision fixes already true in code but missing from prose: "un elemento por línea" and "etiqueta solo en la primera fila de cada sección" (SKILL.md line 256). Everything else in the prior bullet (5 fixed sections, dash for empty, user's language, closing line, walls excluded from the table) verified still accurate, left as-is.

CHANGELOG pass 2026-08-25, second same-day pass (explicit request, scope restricted to `CHANGELOG.md` only, task LIMITS: no other file touched, no commit): new `[Unreleased] → Added` bullet for the new skill `unmassk-groundhog` ("Protocolo Día de la Marmota"). Verified before writing, not transcribed from the task's KNOWN block as-given: read `unmassk-toolkit/skills/unmassk-groundhog/SKILL.md` in full (4-phase read-only inventory→coverage-check→qualify(stable/mechanical/recurrent)→report-and-wait scout, plus a "Creating an approved skill" craft section distilled from Anthropic's official skill-creator — anatomy, 3 loading levels, pushy-description rule, verify-before-handoff step). Confirmed anchored in two of the three claimed surfaces: `unmassk-core/SKILL.md:288` (protocols-menu row) and root `README.md:41` (narrative paragraph) both mention it by name. Root `CLAUDE.md` does NOT literally contain the string `unmassk-groundhog` (checked via grep) despite the task's KNOWN block claiming it's anchored in "el bloque gestionado del CLAUDE.md" — `git diff`/`git status` on `CLAUDE.md` show zero pending changes at pass time, so either the managed-block generator produces the protocols list without repeating skill names verbatim, or that anchor point is not yet real; flagged, not fixed (out of this pass's file scope). Also checked `plugin.json`/`marketplace.json` — grep found zero registration of `unmassk-groundhog` in either, meaning the skill exists on disk and is documented in 2 of 3 surfaces but may not yet be plugin-registered; flagged only, out of scope. Wrote the CHANGELOG bullet directly under `## [Unreleased]` (per the 2026-08-25 first-pass lesson below: never a manual version header, always straight under `[Unreleased]` for `release.py` to promote).

CHANGELOG pass 2026-08-25 (explicit request, version 1.40.0, HEAD at `4a13038` on `main`, task brief supplied its own bullet list — verified every item against code/tests/memory before writing, did not transcribe as-given): covers 2 commits, `562bcbf` (memory legibility+integrity batch, D-056-scoped) + `4a13038` (BREAK-2 hardening fix on the same batch). `[Unreleased]` → new `[1.40.0] - 2026-08-25` section, Added (5 bullets) + Fixed (4 bullets). Verified real via `gitmem search archivada` (D-056's own "Why" — a live nota reading identical to an archived one made a Claude sing a false contradiction) + direct code read: `report_render_note.py`/`report_render_blocks.py` (`archivada` marker + `(↺ old_id)` in listings + `sustituye a {old_id}` in `--id` view), `report_render_chain.py` (`--chain`'s `cerrada`/`sustituida por {id}` literals, docstring confirms cross-zone-lineage fix), `similar.py`'s `_zone_pair()` returning a `frozenset` (unordered-pair fix, BREAK-2 commit), `bin/memory/rule.py`'s `--retract`/`--replaces` argparse wiring + `_KIND_REQUIRED_MSG`, `unmassk-memory-doctor/SKILL.md` (read-only, 8-check content doctor, distinct from `git-memory-doctor`/`gitmem rezones --verify` — said so explicitly). Ran the `--replaces`-missing-new-text case live (`python3 bin/memory/rule.py --replaces "..." --kind user`) — confirmed clean bounce message, no traceback (rc=1). Ran the 5 new/touched test files directly: `pytest .../test_customs_archived_key_zone_duplicate_parity.py .../test_note_exact_key_zone_duplicate_gate.py .../test_rule_retract_replace_contract.py .../test_search_chain_view.py .../test_search_lineage_markers.py -q` = 34 passed. Did not touch `plugin.json`/`marketplace.json`/code/skills/memory beyond this CHANGELOG file, per task LIMITS. No commit made. CORRECTION same pass: orchestrator flagged that `bin/release.py` promotes `## [Unreleased]` to `## [N.N.N] - <date>` itself — content must live directly under `## [Unreleased]`, never under a manually-written version header, or release.py sees an empty [Unreleased] and aborts. Removed the manual `## [1.40.0] - 2026-08-25` header, notes now sit straight under `## [Unreleased]`; rest of file untouched (verified via `git diff --stat` = 2 deletions only). Lesson for future passes: never write a version header manually in this CHANGELOG — always add to `[Unreleased]`, let `release.py` promote it.

CHANGELOG pass 2026-08-24 (explicit request, prep for 1.39.0, uncommitted working tree — 134 modified/untracked paths, HEAD still `582590c`/CHANGELOG last committed at `2a23af0`): `[Unreleased]` written from scratch with Added/Changed/Fixed, one entry per verified working-tree change — did not take the task prompt's own bullet list at face value: checked each claim against the real diff first and corrected several. Verified real: `hooks/checklist-gate.py`+`hooks/skill-checklist-inject.py`+`checklists/*.json` (program-set board, anti-loop max 2 blocks, `stop_hook_active` respected, fail-open); `unmassk-core/SKILL.md`'s one delegation template + blind review template (lines ~185-227) and the closed "on the rare occasion you touch code yourself" contradiction (now "you never touch code yourself"); "excuse" table (literal word) in all 9 `agents/*.md` + `unmassk-memory/SKILL.md` only — NOT in `unmassk-core/SKILL.md` (that file has a differently-named "Red Flags" table instead, already covered by its own bullet, so did not duplicate/conflate the two as the task prompt's wording implied); the false "receives project memory alone" claim removed from all 9 agent cards, `skills:`/`memory:` frontmatter documented as real; scaffolding's 2,776-line figure (818+800+1158 old `best-practices.md`+`frameworks.md`+`wizard-options.md`) matches exactly, split into `references/frameworks/`(10 files)+`references/wizard/`(26 files); the 5 scaffolding inconsistencies confirmed one by one (npm default, per-language verify, CSS generator-support caveats in `wizard/*.md`, canonical single-copy configs for Pydantic settings/path aliases/coverage thresholds, Karma deprecated→Vitest default in `angular-projects.md`) — did NOT find a literal "CSS combo" fix as the prompt described, wrote what the diff actually shows (manual-setup caveats) instead; `rules.py`(559→146)/`zones.py`(338→101) split into `rules_commit.py`/`rules_similarity.py`/`rules_validate.py`/`zones_commit.py`/`zones_load.py`/`zones_query.py`, shared `textnorm.py` (lowercase+NFKD accent-strip) confirmed as the single normalization point per its own docstring (deliberately duplicated once, in `lib/checklist_state.py`, across the `lib/memory` boundary — not a change to correct). `gitmem rule` fix (I-003) verified in `rules_commit.py::commit_or_restore()` (real commit via `notes_commit.stage_and_commit()`, restores/deletes the write on failure) and the revived `health.coherence_rules()` (compares HEAD-committed rules file against the working file, wired into `health.build()`, wrapped in try/except so a corrupt git object degrades to a warning instead of crashing `build()` — same catch also covers `plans_unreflected()`). Did NOT verify or write about: "a user's question is never a go" language in `unmassk-grill`/`unmassk-flow` (checked their diffs — that exact phrasing is pre-existing in `grill.md`, not new, and absent from `flow.md`'s actual diff; the real new `unmassk-core` line about it was already covered by 1.38.0's Modo-automático-era work, not this pass — left out to avoid a false new-in-1.39.0 claim). `plugin.json`/`marketplace.json` confirmed untouched (`git status` shows neither). Previous pass content below preserved as historical record.

Close-mode CHANGELOG-verification pass 2026-08-23 (v1.38.0 published same day; the orchestrator wrote the `[1.38.0]` entry — new owner rule, `.claude/project-memory/rules.md` line 44, "el changelog lo escribe Alexandria, nunca el orquestador" — this pass verifies/corrects it, not the usual full doc-sync scope): found and fixed one real misattribution — the `gitmem work`/`git rm` staged-deletion fix was dated to `[1.38.0]` but `plugin.json` at its fixing commit (`5f6b513`) still reads `1.37.0`, meaning it shipped inside the `1.37.1` release, one release earlier. Moved to `[1.37.1]`, reclassified `Fixed` (was `Changed`) since it's a bug fix, not a behavior change. Re-verified both test-count claims in `[1.38.0]` directly via `pytest --collect-only` (not trusting the entry's own numbers): "51 tests" (`test_skill_router_spanish.py`) and "11 tests" (`test_rule_quote.py`) both exact. Rest of `[1.38.0]`'s content cross-checked against code and left as-is: Modo automático (`unmassk-core/SKILL.md:256`), router Spanish-phrase matching + `[orden]` reminders (`lib/skill_router.py::REMINDER_TRIGGERS`/`match_reminders()`, `hooks/user-prompt-memory-check.py`), `gitmem rule --quote` (`lib/memory/rules.py`, `unmassk-memory/SKILL.md:316-321`), Argus/Bilbo verification-tag story (`agents/argus.md:154`, `agents/bilbo.md:187`, `unmassk-core/SKILL.md:145`), Dante/Ultron losing `Task` (both agent-card `tools:` lines confirmed missing it), R-009 archived (`ARCHIVED.md:27`). See stale-zones.md for the full "Cleared zones (fixed 2026-08-23...)" entry.

Doc-sync pass 2026-08-20 (one capability, phase-only task — sync docs for `stop-dod-gate`'s new exit-code classification, code not touched): `CHANGELOG.md [Unreleased]` got one Changed bullet (exit 5/empty-suite and exit 2/never-written-first-party-module now allow with a once-per-session warning, exit 1 and everything else still blocks, anti-drip signature dedup on block). Three surfaces synced in the same pass: `docs/plan/fix-dod-gate-classification.md` marked `Status: COMPLETED`, all task checkboxes ticked against real evidence (69/69 tests green across `test_stop_dod_gate.py`+`test_dod_gate_classify.py`, `TestDeclaredIdentityD042EndToEnd` class exists), one deviation noted (`git_tracked_status` tri-state helper shipped, not the plan's sketched `is_tracked_in_head` bool) and D-042/X-064 (decided mid-flight per `gitmem search d-042`, not in the plan when it was created) added as D6; `unmassk-toolkit/skills/unmassk-flow/SKILL.md` line ~71 (the only place documenting that Flow writes `test_command` for this hook to consume) got one added sentence on the classification. `README.md` checked and correctly left untouched — no hooks section exists there at all (verified via grep, nothing to update, not an omission). `unmassk-toolkit/skills/unmassk-memory/SKILL.md` hooks table checked — auto-generated, lists only event/matcher/file/timeout (90s, confirmed matches `hooks.json`), never behavior, so no change needed there. Also fixed in passing (explicitly authorized by the task): `tests/test_stop_dod_gate.py`'s docstring had a stale hard-coded class/test count ("11 clases" when it was already 9) — replaced the number with a pointer to `pytest --collect-only` plus today's real count (24/52 in that file, 69 combined with the classifier file), since this exact line has now drifted twice.

Doc-sync pass 2026-08-06 (pre-1.29.2 batch, working tree — 1.29.1 already released same day, `git status` shows the batch as uncommitted changes, no dedicated commit range to diff): `[Unreleased]` got Fixed (5 bullets) + Changed (4 bullets) + Removed (1 bullet), covering `hooks/customs.py` (rescue commands `merge`/`rebase --abort`/`--continue`/`--skip`/`cherry-pick` now approved BEFORE reading `config.json`/`zones.json`, so a corrupt file left by a half-finished merge/rebase can't strand the user; corrupt-file rejection now gives repair instructions instead of a raw traceback; the shlex-fallback path recovers rescue flags from the raw string when tokenizing fails, e.g. an unescaped apostrophe), `hooks/stop-dod-gate.py` (corrupt `config.json` now warns via stderr instead of silent swallow; "not configured" stays silent by design — verified via the diff's own docstring distinguishing `FileNotFoundError` from `(OSError, json.JSONDecodeError)`), `lib/memory/config.py` (docstring clarifies its "never silent" guarantee is `load()`'s own, not inherited by other readers of the same file), `bin/memory/zones.py`+`bin/git-memory-doctor.py` (absent-vs-empty distinction for `zones.json`; doctor gains a zones shape-validation check; `check_project_config` now validates field types), `lib/memory/health.py` (`zones_state()` extracted, called by `_memory_mounted()` and `zones.py list` — verified only 2 direct callers via grep, despite a test-file comment claiming "3 consumidores" that lumps in the doctor's independent reimplementation, which deliberately does not import `lib/memory` and does not call `zones_state()`; wrote the CHANGELOG without asserting a specific consumer count to stay accurate), `lib/install_apply.py` (no longer imports `lib/memory` directly — seeds project-memory indices via `gitmem rezones` subprocess instead, preserving the "memoria v2 must stay deletable" boundary `tests/memory/test_boundary.py` protects), `.github/workflows/{toolkit-ci,plugin-tests}.yml` (permissions read-only, pinned pip deps, actions@v6, concurrency cancel-in-progress, pip cache) + `chatroom-ci.yml` deleted, and `bin/memory/rule.py` (`_parse_args` no longer mutates args mid-parse, moved to `main()`) + `health.memory_mounted`/`possible_unconverted_legacy` privatized. Verified every claim by reading the actual `git diff` for each file (all uncommitted working-tree changes, not yet a commit) before writing — did not take the task brief's wording as given. `skills/unmassk-memory/SKILL.md` + `references/distill.md` were already updated in this same batch (by another concurrent agent) — checked for coherence/duplication: single new paragraph each ("zone is a judgment call, never a script's default"; "Bilbo" named as the distiller), no pre-existing overlapping claim, left untouched. Root `README.md`/`CLAUDE.md` checked (grepped for customs/aduana/doctor/CI/zones.json/version numbers) — no user-facing command, pattern, or convention surfaced by this batch; correctly left untouched (internal robustness fixes to already-undocumented-at-that-depth internals, not new capabilities).

Doc-sync pass 2026-08-02 (fix false promises found by 2 line-by-line reviews, listed in `TOOLKIT.md`'s cleanup list — a follow-up fix pass on the same day as the TOOLKIT.md assembly pass that only flagged these; branch `feat/memoria-v2`, working tree has extensive unrelated uncommitted changes from the memory-v2 rebuild in progress, none of which this pass touched): `[Unreleased]` got one new Fixed section, 4 bullets, one per item in the task brief — see stale-zones.md "Fixed 2026-08-02 (second pass...)" for full detail per file. Did not touch `docs/memoria-v2/`, `docs/spec-sistema-memoria-v2.md`, `DEUDA.md`, or `CLAUDE.md` (explicitly owned by another agent this session). Did not touch `lib/memory/` or `tests/memory/` (5 agents working there concurrently this session, per the task's explicit exclusion). Verified every claim against disk before writing (git-memory-recall.py absence, CALIBRATION.md absence, unmassk-gitmemory skill-dir absence, standards.md's real `^## ` headings, boot_git_checks.py:279 still real).

Doc-sync pass 2026-08-02, third same-day pass (`unmassk-audit/prompts/cerberus.md` fix — self-flagged during the second pass above, then explicitly requested; scope restricted by the task to `unmassk-toolkit/skills/unmassk-audit/` only, no code/tests/`docs/memoria-v2`/`CLAUDE.md`): `[Unreleased] → Fixed` got one new bullet appended to the same-day Fixed section (not a new header). Fixed `prompts/cerberus.md` (both templates: dead `docs/ENTERPRISE-STANDARDS.md` reference, old §-numbering, stale scoring table, auth/routing-specific critical rule), `prompts/argus.md` (OWASP/auth/SQLi focus areas — same external-attacker framing `CLAUDE.md` excludes from this toolkit's threat model — replaced with the internal-integrity surfaces `unmassk-standards/SKILL.md` actually assigns Argus), and `SKILL.md` itself (same 3 issues had leaked into step 4's Argus description, the Findings Report scoring table, and the Additional-Resources OWASP claim — left unfixed here would have recreated the exact inconsistency this pass exists to close). Verified the real scoring table by reading `standards.md` §9 directly before writing (Integrity ×3, Silent-failure ×3, Structure ×2, Real verification ×2, Maintainability ×1). Swept the other 7 prompt templates for the same 2 drift classes — clean, no changes needed. See stale-zones.md "Fixed 2026-08-02 (third pass...)".

Doc-sync pass 2026-07-25/26 (release 1.24.0 prep — trailer-content validation moved to the wrapper + dead validation-hook layer retired, range `032d81c..HEAD` i.e. commits `e2dafbe`/`578177a`/`7437349` on top of the 1.23.0 release commit, orchestration files pre-updated by the orchestrator — `skills/unmassk-gitmemory/SKILL.md`, `agents/*.md`, `skills/unmassk-core/SKILL.md` — NOT touched by this pass): `[Unreleased]` got one Added bullet (`bin/git-memory-commit.py` validates every `Memo:`/`Remember:` trailer's category+shape+non-empty-sanitized-description BEFORE the commit is built, fail-closed; `REMEMBER_CATEGORIES` promoted from a hook-local literal to `lib/constants.py`) + one Removed bullet (`hooks/post-validate-commit-trailers.py` deleted — confirmed 100% dead on the real path; `hooks/pre-validate-commit-trailers.py` cut 233→55 lines, keeping only the live direct-git-commit/log block; `hooks.json` PostToolUse registration removed; `git-memory-doctor.py` `EXPECTED_HOOKS` updated). Verified directly against each commit's diff (`git show <sha> --stat` + reading the resulting files), not from the task prompt's paraphrase alone — confirmed `REMEMBER_CATEGORIES`/`MEMO_CATEGORIES` live in `lib/constants.py`, confirmed `post-validate-commit-trailers.py` no longer exists on disk, confirmed the surviving pre-hook only does the block-direct-commit check. README.md checked (grepped for `hook`/`validat`/`trailer`) — zero mentions of the trailer-validation hooks or a hook count exist there, nothing to correct; left untouched, not forced. Deliberately did NOT frame this as "fixing a pre-existing bug" separately — it's one deliverable (move validation to the producer + retire the dead layer).

Doc-sync pass 2026-07-25 (dead-end memory loop for Bilbo + T1 trailer-newline fix, range `c9939f3..HEAD`, orchestration files pre-updated by the orchestrator — `agents/bilbo.md`, `agents/gitto.md`, `skills/unmassk-core/SKILL.md`, `skills/unmassk-gitmemory/SKILL.md` — NOT touched by this pass): `[Unreleased]` got one new Added bullet (new `deadend` category on `MEMO_CATEGORIES` in `lib/constants.py`; `bilbo` added to the `pre-task-recall.py` injection whitelist, `gitto` deliberately still excluded; the `Memo:` validation error message in `hooks/pre-validate-commit-trailers.py` now generated from `MEMO_CATEGORIES` instead of hardcoded — folded into the same bullet as a supporting detail, not a separate entry per task instruction) + one new Fixed bullet (`bin/git-memory-commit.py`'s wrapper now runs every trailer value through `sanitize_trailer_value()` before writing, collapsing embedded CR/LF so a newline in a value can't silently truncate it on the next read). Verified against `git diff c9939f3..HEAD` directly (17 files, +718/-40), not from the task prompt's paraphrase alone. Deliberately did NOT document the pre-existing "trailer validation dead / parse_trailers broken" front — out of scope, not fixed in this build, tracked separately in git-memory. README.md's Memory row got one added clause (dead-ends Bilbo rules out persist too). No other README section fit (no memo-category list, no per-agent capability table beyond the one-line roster already accurate). No CLAUDE.md base file touched (explicit instruction — council decided against adding a new convention there).

Doc-sync pass 2026-07-19 (atomic CLAUDE.md write fix, T1 — code/tests already shipped per `docs/plan/fix-atomic-claude-md-write.md`, range `f7945f3..HEAD` on `unmassk-toolkit/`, Yoda APPROVED 107/110): `[Unreleased]` → `Fixed` got one new bullet, appended to the pre-existing boot-focused Fixed section (not a 3rd header — the 2-Fixed-headers cosmetic issue, see stale-zones.md, still not restructured). Verified directly against `git diff f7945f3 HEAD -- unmassk-toolkit/` (6 files, +1181/-5): the new opt-in `atomic=True` flag on `open_no_follow_symlink()` (`_AtomicWriteNoFollowSymlink` class in `lib/git_helpers.py` — tempfile.mkstemp same-dir + flush/fsync/close/os.replace, commit only on clean `with`-exit, symlink pre-check, permission-bit preservation with stderr warning on chmod failure, opportunistic >1h orphan sweep) routed through all 4 real writers (`hooks/session-start-crew.py` x2, `lib/install_apply.py`, `bin/git-memory-uninstall.py` — grepped `atomic=True` call sites directly, not from the plan doc's claim). Ran `pytest unmassk-toolkit/tests -q` live: **1129 passed, 2 skipped** (up from 1110), matches task's claimed number exactly. Deliberately did NOT document the pre-existing lost-update race (no file lock, memo `eae0880`) as fixed anywhere — it's an explicit candidate pending Bex's decision, out of scope for this fix. `unmassk-gitmemory/SKILL.md`'s "Filesystem Safety Pattern" section (real coverage gap: the `atomic=True` flag had zero mention despite being live code) got a new paragraph after the existing `open_no_follow_symlink()` cross-platform paragraph, naming the 3 real call sites and noting the lock limitation as known-not-fixed. Root `CLAUDE.md` confirmed fully managed-block-generated (no manual section to touch). `ROADMAP.md` checked — this fix was never listed there (same as #72's precedent) — nothing to close.

Doc-sync pass 2026-07-18 (issue #72 "adelgazamiento" — anti-attacker test cut, doc-only task, code/tests already shipped in `9d43382..HEAD` per `docs/plan/refactor-adelgazamiento-72.md` + fix-pass commits `12389b9`/`2039975`/`33ffaa6`/`3eb21d1`): `[Unreleased]` got one new Removed bullet (the 4 deleted attacker-only files + 6 excised-mixed files, framed against the signed threat model in root `CLAUDE.md`) + one new Fixed bullet (2 integrity-coverage gaps closed same pass: `TestSanitizeTrailerValueControlByteContract` in `test_parsing_consolidation.py`, `TestScopesRenderStaysSingleLine` in `test_boot_output.py`; 15 dead comments across `lib/`/`hooks/`/`tests/` citing deleted filenames corrected). Verified against `git diff 9d43382 HEAD --stat -- unmassk-toolkit/` directly (26 files, -9610/+179; 4 files fully deleted confirmed via `--name-status`), not from the plan doc's prose alone. Ran `pytest unmassk-toolkit/tests -q` live: **1078 passed, 2 skipped** — matches the task's claimed number exactly, no rounding needed. Grepped repo-wide for dangling references to the 4 deleted filenames across `lib/`, `hooks/`, `bin/`, `tests/` — zero stale hits (the one remaining `tests/` mention, in `test_parsing_consolidation.py`'s new docstring, is a legitimate historical note explaining what the new test replaces, not a dangling reference). Root `CLAUDE.md`'s "What security and tests are for in THIS project" section already states the exact policy this work implements — checked, left untouched (already accurate, not touched-for-touching's-sake). `unmassk-gitmemory/SKILL.md`'s symlink/hardlink-guard mentions (`verify_path_within_project()`, `open_no_follow_symlink()`) remain accurate — that production code was never touched by this cut, only the tests' framing. No root/plugin README, SKILL.md, or other CLAUDE.md anywhere in the repo carried a hardcoded test count or "N tests" claim tied to this suite (checked via repo-wide grep) — the only place the 1373→1078 number needed correcting was the CHANGELOG entry itself. `ROADMAP.md` checked — issue #72 was never listed there (not even as a candidate), so nothing to close; left untouched. `docs/plan/refactor-adelgazamiento-72.md` left untouched (historical planning artifact, same treatment as prior plan docs).

Full-mode pass 2026-07-11 (issue #63, boot simplification — pre-merge, branch `feat/issue-63-simplificacion-boot`, Yoda READY 93/110, Cerberus LGTM): `[Unreleased]` got 5 Changed + 5 Fixed bullets covering the branch's user-facing surface, verified against `git diff main..HEAD` directly (not the plan doc): CLAUDE.md's version-only gate (P1) was introduced then retired *within this same branch* (see commits `0b0ff12`/`33de083`/`7842668`) — net vs `main` there is no observable P1 behavior change (session-start-crew.py's exists-first/always-diff logic is identical to `main`'s), so P1 is NOT a CHANGELOG entry; what IS real and CHANGELOG-worthy: `needs_upgrade()`'s Check 1 used a magic string (`"Context Checkpoint Commits"`) that never existed in real canonical content (only test fixtures) so a genuinely current install always looked outdated — fixed by switching to `any_block_outdated()` (pre-existing function, reused not new); the per-message→SessionStart move (point 2, accepted mid-session-`/plugin update` loss); 2 retired pre-v1.0.0 migrations (point 4); `boot_health.py`'s `REPO_BASE_DIR` cache-vs-source bug fix (point 3, confirmed bug not intended behavior per Bilbo's map); the new orphaned-END-marker regeneration in `managed_blocks.py` (deletes only the dangling BEGIN line, never surrounding text — Moriarty T1-1 regression fix within branch); `install_apply.py` no longer stamping the manifest on a failed install; hooks.json SessionStart timeout 30s→45s; `[memory-check]` text shortened ~577→~200 chars. `docs/plan/refactor-boot-simplification.md` left untouched (historical planning artifact, same treatment as prior plan docs). This is a pre-merge branch pass — commits are still `wip`/unsquashed pending Gitto; once merged to `main` the next full pass should re-verify nothing else landed on `main` in the meantime and treat this entry as already covering everything through `cdacab8` (branch HEAD at this pass).

Merge-mode pass 2026-07-11 (issue #61, fast gate only): `[Unreleased]` got 2 Fixed bullets — git-subprocess stderr breadcrumbs (5 prod files: `hooks/precompact-snapshot.py`, `lib/boot_memory.py`, `lib/bootstrap_commits.py`, `lib/git_helpers.py`, `lib/recall.py`, all opting into `run_git()`'s pre-existing `log_stderr_on_failure` kwarg, no return-value/behavior change) + flaky-test-family stabilization (retry+diagnostic pattern in `test_consolidation_trigger.py`/`test_drift.py`/`test_recall.py`, `**kwargs`-tolerant test doubles, 16 new tests in `test_issue61_breadcrumbs.py`). Verified against `git diff ee64aa2..HEAD`. Surface check: README.md/docs/ (excl. docs/plan/)/`unmassk-gitmemory/SKILL.md` grepped for recall/consolidation/snapshot/stderr mentions — the one existing SKILL.md "Fail-open: any error during recall is logged to stderr" line is a pre-existing, unrelated hook-level (`user-prompt-memory-check.py`) fail-open, not affected by this change — confirmed nothing needed updating (diagnostics-only internal change, no new interface surfaced). Issue #61 not yet closed (House root-cause + DoD of 3 green Ubuntu runs still pending) — full audit next pass should re-check whether it closed and whether more commits landed.

Last full audit: 2026-07-10 (ninth pass — issue #60 boot MEMORY stamp relabel + re-source, Yoda GO 101/110). Range documented: `ac3e805..3e971fa` (HEAD at audit time, clean tree per repo git status). Verified against code directly: `lib/boot_git_checks.py`'s `_render_confirmed_fetch_stamp()`/`render_memoria_stamp()` (the literal `MEMORY: remote (synced {age} ago)` / `MEMORY: remote (fetched {age} ago)` / `LOCAL —` texts), the new `lib/boot_fetch_stamp.py` module (own-stamp read/write/rate-limit, remote+branch+URL+schema_version=2 identity, `.claude/.unmassk/boot-fetch-stamp.json`), and `FETCH_TIMEOUT_SECONDS = 10` (comment confirms raised from 3s, decision b2a32b9, already released in 1.19.2 — this pass's [Unreleased] entry is about the stamp relabel/re-source, not the timeout, which was folded into the SKILL.md correction only). [Unreleased] now has Fixed (the `LOCAL`-mislabels-fresh-memory bug) + Changed (2 bullets: FETCH_HEAD→own-stamp re-source, Windows CI Popen-interceptor coverage). SKILL.md `unmassk-gitmemory` updated (see doc-map.md). README.md checked via grep — zero fetch/freshness/FETCH_HEAD mentions found, nothing to fix (confirms Bilbo's finding). Repo-wide grep for "fetch skipped"/"rate-limit"/"FETCH_HEAD" outside `docs/plan/` and agent-memory turned up only the SKILL.md lines already fixed — CHANGELOG.md's own [1.17.0] historical entry still says the old wording/3s/FETCH_HEAD, left untouched deliberately (historical record of what shipped at that version, not a live claim). `docs/plan/fix-boot-memory-stamp.md` and `docs/plan/feat-boot-freshness.md` left untouched (historical, explicit instruction).

Previous full audit: 2026-07-09 (eighth pass — issue #55 date-parsing robustness, wips not yet squashed). Range documented: `0ff8bfe..b3dd092` (HEAD at audit time, clean tree, 13 commits). Verified against code directly: new `unmassk-toolkit/lib/date_parsing.py` (`parse_date()`, extracted from byte-identical duplicates in `git-memory-gc.py`/`git-memory-doctor.py`, now consumes `%at` instead of `%aI`+`fromisoformat()`), the hardening in `lib/boot_git_checks.py`'s `time_ago()` (non-str type guard, `isascii()` gate, length cap before `int()`), `bin/git-memory-doctor.py`'s `check_gc_status()` new `gc_date_unparseable` flag + negative-days clamp, and confirmed `lib/bootstrap_commits.py` was deliberately left on `%aI` (presentation-only field, never parsed). [Unreleased] now has Added (new `date_parsing.py` module) + Fixed (3 bullets: the `%aI`→`%at` degradation fix, the shared-hardening bullet, and the doctor.py `gc_date_unparseable`/negative-clamp bullet with the accepted year-10000-overflow residual folded in, mirroring how 1.16.1 folded its own residuals into Fixed prose). ROADMAP.md checked — issue #55 was never listed there (only #50–#53 appear, under "Frente: deuda técnica del toolkit"); confirmed via `git log 0ff8bfe..HEAD --oneline -- ROADMAP.md` (empty) that this pass didn't touch it either — nothing to strike. No SKILL.md/CLAUDE.md change: this is internal parsing robustness, no new command, flag, or interface surfaced to a future Claude at load; `unmassk-gitmemory/SKILL.md` has zero prior mentions of `%aI`/`%at`/`parse_date`/`time_ago` internals to begin with. Agent-memory-scope commits in the range (cerberus/dante/moriarty/ultron/yoda memory files, `issue-55-date-parsing-contract-notes.md`) correctly excluded from CHANGELOG per project convention.

Previous full audit: 2026-07-07 (seventh pass — issue #49 multi-machine boot memory freshness, GO from Yoda at 107/110, squash pending). Range documented: `d958659..80cf97e` (HEAD at audit time, clean tree). Verified against code directly, not from the plan doc: `fetch_memory_ref()`/`render_memoria_stamp()`/`check_upstream_shares_history()`/`_win32_kill_tree()` in `lib/boot_git_checks.py` and `lib/git_helpers.py`, `resolve_boot_memory()`/`_merge_diverged_memory()` in `lib/boot_memory.py`, the origin-sha cache key in `lib/boot_glossary_cache.py`, `_check_behind_warn_only()` + pre-existing `SUBJECT_MAX_LEN=100` cap in `bin/git-memory-commit.py`, and the two same-day fixes in commit 6fc6386 (`time_ago` `OverflowError`, `_win32_kill_tree` schtasks cross-ref docstring note — confirmed the latter is a comment-only change, not a behavior fix, so it was folded into the Added feature's "known limitation" prose rather than mislabeled under Fixed). SKILL.md `unmassk-gitmemory` updated: new "Multi-machine freshness" paragraph in Boot Protocol, new bullet in Wrapper Scripts for the warn-only behind notice. README.md Memory row got one clause. No root CLAUDE.md change needed — `lib/managed_blocks.py` has no boot-freshness-specific content to regenerate, and the existing generic "read SessionStart output" instruction already covers the new MEMORY:/PULL DIRECTIVE lines without modification. `docs/plan/feat-boot-freshness.md` is an internal Flow-pipeline planning artifact (not a Diátaxis doc for the three-audience rule) — left untouched, same treatment as roadmapv2.md-only edits.
Last commit covered (toolkit root, sixth pass): 51a3c44 (decision: F6 hard-link bypass deferred past v1.16.1) — HEAD at time of this audit. The fix itself (`git_helpers.py`, `_symlink_safe_open.py`, `version.py`, `boot_health.py`, 4 hooks, `skill-search.py`) is UNCOMMITTED working-tree changes at audit time, documented into [Unreleased] ahead of the commit/release so `bin/release.py` has content to promote. Verified every changed file's diff against HEAD directly (not from memory): `os.O_NOFOLLOW` Windows branch in both twins (`open_no_follow_symlink`/`open_no_follow_symlink_fallback`), `encoding="utf-8"` added to `run_git`, `version.py`, `boot_health.py` (3 subprocess/open sites), 4 hooks, and `os.pathsep` in `scripts/skill-search.py`. Previous audit (fifth pass, f6cc6ac) superseded by this one.
Last commit covered (chatroom): f4196fa — not re-checked this pass, no chatroom commits in the range reviewed.
Current version in plugin.json: v1.15.0 released (chore f1dcb8e). [Unreleased] now covers the 2026-07-05 boot-hook incident: started as a truncation bug (SessionStart stdout losing the `Next:` instruction on large briefings), escalated via a 14-round Cerberus+Argus audit into 3 real security fixes (parent-directory `.claude` symlink escape closed by new `verify_path_within_project()` chokepoint in `lib/git_helpers.py`, 9 call sites; untrusted git-commit content reaching Claude's context unsanitized at several render sites; control-byte log-injection via forged `\x1e`/`\x1f` separators, fixed with `git log -z`) plus a structural refactor (session-start-boot.py 1278→330 lines + 6 lib modules; git-memory-bootstrap.py 936→143; git-memory-install.py 541→252; doctor.py/upgrade.py accepted as documented 500-line-limit exceptions). Verified against code: `verify_path_within_project()` docstring/9 call sites, `_sanitize_trailer_value` (21 call sites), `git log -z` (2 sites in boot_memory.py), all line counts via `git show <old-commit>:<path> | wc -l` vs current. SKILL.md `unmassk-gitmemory` updated: confirmed the existing "boot stdout always banner" note was already accurate (no change needed there), added a new "Filesystem Safety Pattern" section documenting `verify_path_within_project()` as canonical for any new `.claude/` filesystem code. ROADMAP.md checked — no stale/contradictory entries (this was unplanned incident work, correctly absent from roadmap).

Doc-sync pass 2026-07-13 (new plugin `unmassk-pentesting` v1.0.0, human-facing docs only — code/skills/marketplace.json/PROVENANCE.md already shipped in commits `b2f5788`..`4b62d83`, HEAD clean): `[Unreleased]` got one Added bullet for the plugin. Verified against the tree directly, not from the task prompt alone: 30 skill dirs under `unmassk-pentesting/skills/`, 743 total `.md` (30 `SKILL.md` + 713 `references/*.md`, 0 `.py` — matches commit `ad00b94`'s own count), `plugin.json` v1.0.0 registered in root `.claude-plugin/marketplace.json`, `PROVENANCE.md` pins upstream commit `e659245c` (communitytools by Transilience AI, MIT). Read `pentesting-engagement/SKILL.md` + `references/{method-loop,scope-authorization,blind-validation,crew-roles}.md` directly to describe the method (SCOPE allowlist contract, two-key rule, blind refuter+reproducer quorum, per-claim mechanism-vs-severity verdict reconciliation) instead of trusting the task's paraphrase. Confirmed the "2/30 skills verified live" claim against git-memory decision `470f4da` (real target `checks.monyma.es`, a Laravel app of Bex's, black-box authorized — auto-trigger loaded the plugin-installed skill, not memory; blind validation ran with real curl output; one candidate finding was mechanism-CONFIRMED but severity-REFUTED 2/2, correctly reported Informational not a vuln) and the still-open risk against decision `109ec0e` ("probar trigger antes de cualquier release" — only recon+injection tested per the `d35cfe6` Next-note plan, other skills' triggers not yet swept). Also updated root `README.md` (install command + "What's inside" table row, 30 skills) — was a genuine coverage gap, the plugin was registered in marketplace.json but absent from the human-facing repo README. Wrote `unmassk-pentesting/README.md` from scratch (none existed) — see doc-map.md for what it covers. Did not touch skill content, frontmatter, or `PROVENANCE.md` (out of scope, already correct).

Doc-sync pass 2026-07-14 (unmassk-design multi-branch revamp, doc-only task — skill content/plugin.json already shipped, not touched): `[Unreleased]` got one Changed bullet: core (1 skill) → 7-skill plugin, 6 new branches (design-motion, design-3d, design-scroll, design-animation-formats, design-taste, design-flutter) with sources/licenses named inline. Verified against each branch's own `SKILL.md` frontmatter + Attribution section directly (not the task prompt's paraphrase): design-motion pulls from 4 sources (emilkowalski/skills' 5 sub-skills, motion-dev-animations-skill, claudedesignskills' motion-framer+react-spring-physics, css-animation-skill's walkthrough-generator); design-3d/design-scroll/design-animation-formats all condense claudedesignskills (10+4+4 sub-skills respectively); design-taste condenses taste-skill's 8 sub-skills; design-flutter adapts claude-flutter-ui-skills' flutter-ui (state-mgmt/navigation deliberately left out, confirmed via its own "Out of scope" section). Created `unmassk-design/PROVENANCE.md` + `CREDITS.md` (new plugin-level files, didn't exist before — same treatment as unmassk-pentesting/unmassk-humanizer). Rewrote `unmassk-design/README.md` (7-skill overview table + rescoped core sections + updated Skill routing/Dependencies/Attribution). Root `README.md`'s unmassk-design table row updated (1→7 skills, description now names all 6 branches). Did not touch skill content or `plugin.json` (already shipped, out of scope per task). Flagged but did not fix (orchestrator-owned per 2026-07-12 precedent): `unmassk-core/SKILL.md:40` still says "unmassk-design | 1 skill" — see stale-zones.md.

Doc-sync pass 2026-07-16 (MCP on-demand pattern replicated to 6 plugins, doc+config task, no code touched): `[Unreleased]` got one new Changed bullet — `unmassk-electronics`, `unmassk-frontend`, `unmassk-compliance`, `unmassk-marketing`, `unmassk-media`, `unmassk-seo` `.mcp.json` all emptied to `{"mcpServers": {}}`, matching the `unmassk-3d` pilot (verified by reading `unmassk-3d/.mcp.json` + `blender-mcp.md`'s "Activate the MCP" section directly, not from memory, before replicating). Added an "Activate the MCP" section (register → restart → key) to: `electronics-micro/references/setup.md` (platformio), `frontend-react/references/agent-browser.md` (agent-browser), `compliance-i18n/references/mcp-integration.md` (better-i18n), `unmassk-marketing/skills/unmassk-marketing/SKILL.md` (composio), `media-image-gen/SKILL.md` (media-pipeline/Gemini), `unmassk-seo/skills/unmassk-seo/SKILL.md` (all 5 SEO servers in one block, per task instruction). Corrected stale "already declares/configures/registers" phrasing found in the same files plus `unmassk-marketing/README.md` and `unmassk-seo/README.md` (both said "the plugin configures N MCP servers"). Did NOT touch `unmassk-3d`'s own CHANGELOG `[Unreleased]` entry (line 12) even though its wording ("driven live over an MCP bridge... wired in `.mcp.json`") is now stale against the pilot's actual empty `.mcp.json` — out of scope for this task (only the 6 new plugins were requested), flagged in stale-zones.md instead. Same for the `unmassk-frontend` AgentBrowser entry (line 9, "wired via a plugin `unmassk-frontend/.mcp.json`") — technically still accurate in that the file exists and the wiring mechanism (claude mcp add) is what makes it work, but doesn't mention on-demand; left as-is, flagged.

Doc-sync pass 2026-07-18 (issue #61 fix, "pérdida silenciosa de memoria en fallo transitorio de git" — code/tests already shipped per `docs/plan/fix-silent-memory-loss-61.md`, range `a90c9d7..HEAD` on `unmassk-toolkit/`, Yoda APPROVED): `[Unreleased]` → `Fixed` got one new bullet (appended to the pre-existing boot-focused Fixed section, not a 3rd Fixed header) naming all 9 production read sites now wrapped by `run_git_read_retrying()` (`lib/git_helpers.py`): `recall._scan_commits`, `boot_memory.extract_memory`+`extract_glossary`, `boot_git_checks.get_timeline`+`get_last_context_time`, `git_helpers.commits_since_last_consolidation`, `bootstrap_commits.scan_recent_commits` (2 calls — commits + authors), `precompact-snapshot.extract_memory_from_log`. Verified the "9" figure against `git diff a90c9d7 HEAD` directly (10 call sites across 8 functions, bootstrap's 2 calls counted as 2 sites per the team's own commit messages `a9c23aa`→`3e9835d`), not from the plan doc's summary alone. Ran `pytest unmassk-toolkit/tests -q` live: **1110 passed, 2 skipped**, matches the task's claimed number exactly. Confirmed `test_issue61_read_retry_contract.py` has exactly 32 `def test_` (task's claimed "32 tests"). `unmassk-gitmemory/SKILL.md` updated: new "### Transient Read Resilience (issue #61)" subsection under Recovery, right after "Passive Healing" (explicitly scoped as orthogonal — Passive Healing is about *what* history is read, this is about *surviving a flaky read* of it); the pre-existing "Recall gatekeeper" fail-open line got one clause pointing to the new subsection instead of duplicating it. Grepped repo-wide for `#61`/`flaky`/`flakiness`/`Ubuntu` outside `docs/plan/` and agent-memory — found nothing else needing correction (no README, root CLAUDE.md, or other SKILL.md made any claim about this). Deliberately did NOT frame #61 as fully closed anywhere — the fix covers all 9 read sites (bootstrap included) but issue closure itself is not a documentation claim this pass makes.

Root CHANGELOG structure note: Three product timelines merged into one file. Old git-memory [1.1.0] entry was renamed [1.1.0-gitmemory] on 2026-03-24 to avoid collision with toolkit [1.1.0].

[Unreleased] now has (2026-07-04, commits 278b41b + 4042f28):
- Added: project startup quality floor — `unmassk-project-lifecycle`'s START branch (`unmassk-toolkit/skills/unmassk-project-lifecycle/SKILL.md`) gains a step, before the first feature commit on a new project, confirming a working test command, lint/format config, and a `.env.example` (if secrets are implied). Missing pieces must be logged as `decision()`, not silently skipped. A small slice of the frozen "solid project startup guide" roadmap idea that a validation council found didn't depend on the memory/consolidator maturity gate the rest of that idea is waiting on.
- Excluded (by design, see precedent note below): 278b41b also added a "roadmap worked in written order" discipline to `roadmapv2.md`'s "Cómo trabajamos" section. This is a `roadmapv2.md`-only edit (internal project bookkeeping), not a shipped product change — left out of CHANGELOG, already covered by the roadmap edit itself + the `decision()` git-memory commit.

**Precedent confirmed this pass — roadmapv2.md-only edits never go in CHANGELOG:** scanned full `git log -- roadmapv2.md` history (20+ commits touching only that file: adding/removing candidates, marking items done, reordering). None ever appear as individual CHANGELOG entries. Contrast with the 1.12.0 "commit/push cadence" entry, which WAS included — that one changed real shipped `unmassk-gitmemory`/`unmassk-flow` SKILL.md behavior, not just a roadmap note. Applied this distinction to exclude the roadmap-order-discipline item above.

- No prior-pass entries survive here — the previous [Unreleased] content (skill-router nudge, protocols menu extension, frontmatter collision fixes) was correctly promoted to [1.14.0] by release.py; confirmed the section reads correctly in CHANGELOG.md before writing this pass.

SKILL.md updated: Crown row added to Trailer Spec table; "Memory consolidation trigger" bullet added to Active Hooks; new "Crown entries (👑)" section added between Active Hooks and Hierarchical Scopes.

[1.6.0] section covers (2026-06-10):
- Added: hard DoD gate (hooks/stop-dod-gate.py, 23 tests)
- Changed: unmassk-core hardened (no trivial code edits by orchestrator)

[1.5.0] section covers (2026-06-10):
- Added: memory dedup gate (hooks/pre-memory-dedup-gate.py, 40 tests)
- Changed: memory capture reminder flipped to restraint

[1.4.0] section covers (2026-06-09):
- Added: release script (bin/release.py + helpers + --path flag + docs/RELEASING.md) + documentation coverage improvements
- Fixed: scope-map path in unmassk-gitmemory SKILL.md, test isolation bug in test_migrate_statusline.py
- Removed: dead weight (!new_skills/, generated-images/, orphaned .pyc)

[1.3.0] section covers (2026-06-08):
- Added: recall gatekeeper hook (pre-task-recall.py, 51 tests), build-mode (Flow router + linear/test-first references + Ultron/Dante awareness), CLAUDE.md block generator (lib/managed_blocks.py, 35 tests), 4 protocol skills installed (close-session/grill/council/lifecycle), close-session Stop hook, PRD template, communication block in CLAUDE.md
- Changed: Flow skill (routes to references/ instead of inlining), memory calibration tightened (anti-over-saving, repo-type reframe), unmassk-audit aligned with repo_type and coverage gate decisions, core skill clarified (Ultron=prod code only)
- Fixed: boot hook redundant dump removed, flow-stack scaffold path corrected

[1.2.0] section covers (2026-06-05):
- Added: recall engine (lib/recall.py + CLI)
- Changed: run_git cwd param, constants.py extraction
- Removed: context-tracking subsystem
- Fixed: upgrade self-heal for stale statusline
- Security: shell=True eliminated

[Unreleased] chatroom CHANGELOG.md: still contains V2 agent prompts, 5-phase pipeline, file attachments, LOC refactor, mention-parser fix, stoppedRooms guard — not yet versioned/released (unchanged from 2026-03-24).

Doc-sync pass 2026-08-22 (uncommitted working tree at pass time, HEAD `6b416f8`, `CHANGELOG.md` last committed at `885e1b7` — D-044/D-045 memory decisions): `--issue` on `gitmem note` opens from M-only to all seven types (D/M/R/Q/X/I/B), with the exists-check gate (`validator_issue.py::validate_issue`, already type-agnostic, unchanged) and the boot counter relabel "plans with a record" → "issues with a live note" (`boot.py:392`, still zero GitHub calls — Argus's invariant intact). Verified fully shipped, not just Dante's red contract: `pytest unmassk-toolkit/tests/memory/test_note_issue_field.py` = 17 passed (Dante's note said 7 red/10 green at contract time — all green now). Added `[Unreleased]` → Changed, 2 bullets (the --issue opening + the new `references/issues.md` reference doc). Root `README.md` and `docs/` checked — neither describes memory command syntax at this granularity (README is narrative-level only), correctly left untouched. Root `CLAUDE.md` checked for stale claims about this feature — none found, "issue #83" claim it makes is about a different, already-closed GitHub issue, unrelated to this note-field change.

**Why:** Alexandria needs to know where to resume on next launch — only commits after the covered commits need processing.
**How to apply:** On next run: `git log 3e971fa..HEAD --oneline` for toolkit root (supersedes the old `b3dd092..HEAD` marker — this pass's #60 entry already covers everything through 3e971fa); `git log f4196fa..HEAD --oneline -- chatroom/` for chatroom. Check for new code changes not yet in either CHANGELOG.md. Note: #49 (multi-machine boot freshness) and #55 (date-parsing) are already released ([1.17.0], [1.18.0]) — confirmed in CHANGELOG.md before writing this pass, not stale. [Unreleased] now holds only the fresh #60 stamp-relabel entries (Fixed + Changed), waiting on the next squash/`release.py` run (plan doc notes it will promote to 1.19.3). Also check `git status`/`git diff HEAD` for uncommitted working-tree changes, not just `git log`.

Doc-sync pass 2026-07-12 (BM25 skill-search gate retirement, doc-only task, working-tree changes still uncommitted at pass time — commit `5d660b4` is the decision, tag `bm25-skill-gate-1.19.9` is the archive point): `[Unreleased]` got a new Removed bullet. Verified against `git status`/`git diff --stat` directly, not from the task prompt alone: `scripts/skill-search.py` deleted, 36 `catalog.skillcat` files deleted (`find` confirms zero remain on disk), `pre-task-recall.py` had 223 lines of skill-gate logic stripped (8 inserted) with memory-recall injection confirmed intact. Rewrote 7 plugin READMEs' "## BM25 skill discovery" → "## Skill routing" sections (unmassk-db, unmassk-media, unmassk-ops, unmassk-marketing, unmassk-seo, unmassk-compliance, unmassk-design — design's own separate BM25 corpus engine for `search.py`, lines 11/22, deliberately left untouched), root `README.md:52` "What changes" table row, and the two domain-skill framing lines in `frontend-standards.md`/`typescript-strict.md` that said "discovered via skill-search". Also marked `docs/plan/fix-windows-crossplatform.md`'s F6 bullet MOOT (pointed at a now-deleted file). Did not touch `unmassk-core/SKILL.md`, `unmassk-gitmemory/SKILL.md`, `unmassk-audit/SKILL.md`, or any `agents/*.md` — orchestrator owns those. Two known contradictions flagged but not fixed (out of scope, orchestrator's to resolve): `unmassk-core/SKILL.md:44` vs `:77` (self-search claim vs gate claim, both now stale against the new no-BM25 reality — not just contradicting each other), `unmassk-audit/SKILL.md:273` (says unmassk-standards loads "via BM25 skill search", actually loads by frontmatter).

Merge-gate pass 2026-08-05 (`feat/memoria-v2` -> `main`, mode=merge, pre-publish gate): added `[Unreleased]` Added/Changed sections covering the whole branch -- the new `gitmem` 9-subcommand memory system (facade `bin/gitmem`, scripts in `bin/memory/`, library in `lib/memory/`, two zones per note), `lib/memory/remote.py` + `timefmt.py`, Alexandria's new `close`/`foundation` modes, the close-session rework (4 steps, transcript-reading agent, `scripts/session_transcript.py`), the boot rewrite (fetches remote, writes report to `.claude/.unmassk/boot-latest.txt` instead of injecting), and the 4 skills (flow/project-lifecycle/council/grill) that dropped calls to the retired `git-memory` system (audit needed no change, confirmed already agnostic). Did not touch or remove the pre-existing 5 Fixed bullets from the 2026-08-02 doc-sync pass (unmassk-audit prompts, unmassk-flow references, unmassk-close-session frontmatter, README/marketplace.json) -- one of those bullets (the close-session one) describes an intermediate state now superseded by the full rework; left as historical record rather than silently edited, flagged to the user instead. Real code on the branch is almost all squashed into one commit (`5e682b4`); the other 195 commits are `decision`/`memo`/`context`/`remember` trailer commits (git-memory's own audit trail) plus roughly 140 synthetic `[M-0XX]` test-fixture commits from `notes-test` work -- none of that is changelog material and was correctly excluded. **Found and reported, not fixed (owner-owned):** the root `CLAUDE.md`'s WIP header (lines 9-15) is now stale in two places -- it claims the managed block "was just regenerated right here" dropping the `unmassk-gitmemory`/`CALIBRATION.md` boot steps, but the live block (lines 103-120) still instructs loading `skill="unmassk-gitmemory"` and reading `CALIBRATION.md`, both deleted from disk on this branch; and it claims "nothing in this work is committed" / 5 of 10 docs are untracked, but `git ls-files` + `git status --porcelain -uall` confirm everything (`DEUDA.md`, all of `docs/memoria-v2/`) is tracked and committed as of `5e682b4`.
