# Yoda Memory — unmassk-toolkit

## Topic files
- [judgment-patterns.md](./judgment-patterns.md) — patrones de juicio aprendidos. Empieza por su "Nota de vigencia" (2026-08-25): el sistema de memoria/boot anterior que documentan la mayoría de las entradas fue borrado del repo (2026-08-05 y 2026-08-23) — verificar que un fichero sigue existiendo antes de reusar su cita.
- [conventions.md](./conventions.md) — convenciones del proyecto, incluye el incidente de git-safety propio (antes en lessons.md, fusionado 2026-08-25) y las notas de vigencia post-reescritura memoria-v2.

## Session log
- 2026-06-09: Primera evaluación. Script de release (release.py + release_helpers.py + bump-version.py). Veredicto: READY. Ver judgment-patterns.md.
- 2026-07-05: Juicio final del pipeline de hardening del boot hook (14 rondas Cerberus/Argus, sin Moriarty separado). Reproduje en vivo 2 ataques críticos (symlink en settings.json, inyección de registro vía bytes de control en git log) y un mutation test. Veredicto: APPROVED WITH CONDITIONS. Ver judgment-patterns.md y conventions.md.
- 2026-07-06: Juicio del fix cross-platform Windows (guard anti-symlink O_NOFOLLOW->hybrid, encoding utf-8 barrido, decisiones 013b064/75fdb2f). Reproduje en vivo el Round-Trip Sabotage (§34) yo mismo (Moriarty no dejó evidencia de haberlo hecho para este seam) y el hard-link bypass de Moriarty. Verifiqué los ~77 fallos preexistentes con git stash (9/9 reproducidos en pre-patch). Veredicto: APPROVED (96/110). Ver judgment-patterns.md y conventions.md.
- 2026-07-06 (ronda 2): Re-evaluación del mismo fix cross-platform tras condiciones cumplidas (doc F6 simétrica, decisión formal 51a3c44 de diferir cierre de F6, fix de stderr para UnicodeDecodeError en run_git, Moriarty ejecutó su propio Round-Trip Sabotage y Dante arregló el test-teatro que Moriarty destapó). Repetí el sabotaje de encoding yo mismo (parché run_git, confirmé rojo con mojibake real, restauré, confirmé verde). Encontré también un fix no listado por el orquestador (fd leak + destructive-truncate-before-check en el guard Windows) leyendo el diff directamente. Veredicto: READY WITH CONDITIONS (101/110), condición única no bloqueante = issue de diseño para cierre de F6. Ver judgment-patterns.md.
- 2026-07-06 (ronda 3): Juicio final de frescura multi-máquina del boot (issue #49, fetch endurecido/gateado/rate-limited + sello MEMORIA + lectura de origin + guard T2 de repo-identity via merge-base). Corrí yo mismo 107/107 (suite de la feature) y 979/979 (suite completa). Verifiqué mecánicamente los 4 hechos del gate §34 (round-trip con bare remote real, valores derivados del productor, misma sanitización -z/NUL para origin que para HEAD, fail-open sin auto-pull). Veredicto: APPROVED WITH CONDITIONS (100/110) -- 3 huecos de Windows (kill-tree, askpass cmd/c, fetch endurecido) revisados por lógica pero NUNCA ejecutados en Windows real, declarados explícitamente como riesgo aceptado. Ver judgment-patterns.md.
- 2026-07-18: Juicio final issue #61 (perdida silenciosa de memoria por git rc!=0 en read-path). Verifique yo mismo: suite completa 1107 passed/2 skipped exit 0, contrato 29/29, round-trip Section34 real (commit real + fallo real rc=128 via directorio roto + recuperacion), y reproduje en vivo el mutation-kill del fix de latencia SEC-HIGH-001 (revert del cap por intento -> mismo test rojo que Moriarty). Encontre GAP de completitud no visto por nadie en el pipeline: lib/bootstrap_commits.py:126,150 (scan_recent_commits, git memory bootstrap) sigue colapsando rc!=0 sin retry pese a estar marcado con comentarios 'breadcrumb #61' propios desde una pasada de House anterior a este plan. Veredicto: APPROVED WITH CONDITIONS (92/110) -- boot-critical path cerrado, bootstrap_commits.py como follow-up Minor no bloqueante. Ver judgment-patterns.md.
- 2026-07-09 (re-veredicto): Issue #53 close-out tras condiciones 2 (try/except create_backup) + 3 (cross-ref doctor.py<->boot_glossary_cache.py). CI verde ambos runners verificado via `gh run view` (headSha=7c3624f), suite local corrida por mi 987 passed/77 skipped/0 failed. Reproduje en vivo un hard-link real contra create_backup() con la logica exacta de main(): capturado, mensaje formateado, exit 1, victima intacta -- pero encontre un comentario recien escrito (mismo commit) que afirma FALSAMENTE que UnsafePathError no se captura (SI se captura, UnsafePathError subclasea OSError). Veredicto: APPROVED (GO), 96/110 -- no 110 literal, 3 items exactos listados (2 comentarios de precision + 1 hueco de test del try/except nuevo). Ver judgment-patterns.md.
- 2026-08-20: Juicio final stop-dod-gate.py (clasificacion exit 5/1/2, D-042 identidad declarada, git tri-state, SIGHUP). Reproduje en vivo D-042 (ambos lados), la corrupcion real de .git/index, el round-trip del fichero de estado (2 sabotajes), y confirme por control experimental que un fallo SIGHUP en la suite completa era un artefacto de MI PROPIO uso de `nohup` (no un bug real -- se corrige backgroundeando con `run_in_background` del propio harness). Encontre yo mismo un Major no visto por nadie del pipeline: `_run_test_command()` trata `UnicodeDecodeError` (subclase de ValueError) sobre salida no-UTF8 de un fallo REAL como fail-open silencioso -- pre-existente, fuera del scope D1-D5, no bloqueante de este diff pero condicion urgente nombrada. Veredicto: APPROVED WITH CONDITIONS, 85/110. Ver judgment-patterns.md.
- 2026-08-22: Juicio final `--issue` abierto de M a los siete tipos de nota (D-044/D-045). Verifique yo mismo 508/1 en tests/memory, escribi y borre un test manual propio de ida y vuelta (by-id/zona/palabra) contra un repo temporal real, y ejecute un sabotaje en vivo de `_incident_block` en `report_render.py` (comente la linea de Issue, confirme rojo real en 3 tests, restaure, confirme verde) -- mutation-kill genuino, no narrado. Consulte `gitmem search` para D-043/D-044/D-045/X-065..070 antes de juzgar: confirme que ningun wall se viola y que "ningun contador en el arranque" de D-044 se refiere a X-066 (un contador NUEVO descartado), no al `open_issues` preexistente que la feature solo re-etiqueta. Encontre, fuera del diff de la feature pero en la misma working tree, corrupcion real (no sospecha) de `.claude/agent-memory/unmassk-toolkit-dante/MEMORY.md` -- ~102 lineas preexistentes truncadas a mitad de palabra. Veredicto: APPROVED WITH CONDITIONS (104/110) -- la feature en si sin blockers, condicion = restaurar el MEMORY.md de Dante antes de comitear el lote. Ver judgment-patterns.md y conventions.md.
- 2026-08-24: Juicio final I-003 (rules.py split en rules_commit/rules_similarity/rules_validate + resurreccion de coherence_rules) + checklist-gate/skill-checklist-inject (D-052) + textnorm.py (unificacion D-054). Corri la suite completa yo mismo (1183 passed/2 skipped/0 failed) y ejecute un sabotaje en vivo del fix compartido MM-state en notes_commit.py (mutation-kill genuino, restaurado byte a byte). Veredicto: APPROVED, 106/110. Ver judgment-patterns.md.
- 2026-08-25 (compactación de memoria, no un veredicto): lei los 4 ficheros enteros (858 lineas) y contraste contra el repo real con `find`/`git log --diff-filter=D`/`gh issue view`. Hallazgo principal: el grueso de las entradas 2026-06-09..2026-07-25 + 2026-08-06/08-20 juzgan el sistema de memoria/boot ANTERIOR, borrado del repo (2026-08-05 commit 615f5cc, y stop-dod-gate.py aparte el 2026-08-23 commit 5f6b513) -- marcado con nota de vigencia al inicio de judgment-patterns.md en vez de borrar (la tecnica de verificacion sigue siendo valida, el fichero concreto no). lessons.md (1 entrada, huerfano del indice de MEMORY.md) fusionado dentro de conventions.md y borrado. issue #64 confirmado CLOSED via gh. doctor.py confirmado en 714 lineas (era 518). Ver judgment-patterns.md (nota de vigencia) y conventions.md.
- 2026-08-27: Juicio final del plugin `unmassk-trading/` fase 1 (leer/practicar/dimensionar; ejecucion en vivo documentada pero no ejercitada). Corri yo mismo 645/645, 5 mutaciones vivas sobre `price_check.py` (todas muertas, sha256 restaurado) + 2 sabotajes del PRODUCTOR real (endpoint y campo de Kraken) que mataron el round-trip live, verifique el "byte a byte" de los 10 artefactos levantados contra el repo origen clonado (cabecera de atribucion + 1 linea de logica, exactamente lo declarado), y ejecute las ~14 afirmaciones marcadas "verified" en la prosa (las 14 se sostuvieron, hasta las cadenas literales de razones del gate). Encontre: CI "en rojo" que en realidad es el job de los plugins maker (numpy 2.4.6 / py3.10), con el job de trading VERDE — la decision de separarlos quedo validada por ese mismo run; y BREAK 6 de Moriarty (carrera lost-update en `thesis_store.link_report`) como el unico de sus seis sin rastro en ningun sitio del plugin. Veredicto: APPROVED WITH CONDITIONS (96/110). Ver judgment-patterns.md.
